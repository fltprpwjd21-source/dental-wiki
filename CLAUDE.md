@AGENTS.md

# 치과위키 프로젝트 규칙 (초안)

## 프로젝트 개요
- 치과 스탭(치과위생사, 기공사, 방사선사) 전용 사내 위키 + LLM Q&A 서비스
- 진료과별 인수인계, 보험 수가·비보험 항목, 병원 내규·운영회칙 3개 카테고리 문서를 다룬다
- 자세한 배경·기능·범위는 [PRD.md](PRD.md) 참고

## 작업 규칙 (AI 협업 규칙)
- 모든 설명과 주석은 한국어로 작성한다
- 새 파일은 `my-app` 폴더 안에만 만든다
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다
- 파일을 지워야 할 때는 바로 삭제하지 말고 `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 최종 삭제는 사용자가 직접 확인 후 진행한다
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다

## 기술 스택
- Next.js (App Router, TypeScript, Tailwind CSS, ESLint) — 화면과 API를 한 프로젝트에서 처리, 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다
- `src/` 디렉터리 미사용, import alias는 `@/*`
- 배포: Vercel

## 폴더 구조
- `app/` — 페이지 및 API 라우트
- `.env` — 비밀 값 보관용 (절대 커밋 금지, `.gitignore`에 등록됨)
- `PRD.md` — 기획서. 기능·범위가 바뀌면 함께 갱신한다

## 보안 규칙
- API 키, 토큰 등 민감한 값은 `.env`에만 저장하고 서버 코드에서만 사용한다. 클라이언트 컴포넌트나 브라우저로 절대 노출하지 않는다
- `.env`와 `node_modules`는 `.gitignore`에 등록해 두고 절대 커밋하지 않는다
- 환자 개인정보·진료기록 등 개인 식별 정보는 코드, 테스트 데이터, 위키 문서 어디에도 포함하지 않는다
- 로그인은 "사원번호(화이트리스트) + 공통 비밀번호" 방식이다. 화이트리스트 등록·삭제는 설정 탭의 관리자 옵션에서만 가능하게 구현한다

## 코딩 컨벤션
- TypeScript strict 모드 유지, `any` 사용은 피한다
- 컴포넌트 파일명은 PascalCase, 함수·변수명은 camelCase
- 커밋 전 `npm run lint` 통과를 확인한다

## 작업 절차 (검증 루프)
코드를 바꿀 때마다 아래 순서를 통과할 때까지 반복한다.
1. 변경한다
2. → `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다
3. → 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다
4. → 문제가 있으면 고치고 다시 1)로 돌아간다
5. → 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다

## 기능 개발 순서
- PRD.md의 "개발 단위" 순서를 따른다: 1차 로그인+Q&A → 2차 수정·로그·되돌리기 → 3차 카테고리별 탐색 → 4차 설정 탭 관리자 옵션
- 문서 수정 기능은 반드시 자동 로그 기록과 함께 구현하고, 완전 삭제 기능은 만들지 않는다 (수정만 가능, 이전 버전은 로그로 보존)

## 이번 프로젝트에서 만들지 않는 것
- 승인/결재 절차, 역할별 세분화 권한(설정 탭 관리자 옵션 제외), 통계 자동 집계 대시보드, 모바일 전용 앱, 멀티테넌시
- (자세한 내용은 PRD.md의 "범위와 비범위" 참고)
