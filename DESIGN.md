# DESIGN

PLAN.md의 작업 목록을 화면·데이터 흐름·기술 관점에서 정리한 설계 문서입니다.

## 1. 화면 구성 (무엇이 어디에 보이는지)

| 화면 | 보이는 것 |
|---|---|
| **로그인 화면** | 사원번호 입력칸, 공통 비밀번호 입력칸, 로그인 버튼 |
| **Q&A(홈) 화면** | 상단에 질문 입력창, 아래에 AI 답변 + 근거 문서 링크, 좌측/상단에 3개 카테고리 바로가기 |
| **카테고리별 탐색 화면** | 진료과별 인수인계 / 보험·비보험 수가 / 내규·운영회칙 3개 탭, 탭 안에 문서 목록, "새 문서 만들기" 버튼 |
| **새 문서 등록 화면** | 카테고리 선택, 제목 입력칸, 본문 입력칸, "등록" 버튼 |
| **문서 상세·수정 화면** | 문서 본문, "수정하기" 버튼, 하단에 접혀있는 "수정 로그 보기"(펼치면 이전 수정 이력과 되돌리기 버튼 표시) |
| **설정 탭 (관리자 전용)** | 화이트리스트에 "관리자"로 표시된 사원번호로 로그인했을 때만 보이는 메뉴, 사원번호 화이트리스트 등록·삭제 목록 |

모든 화면 상단에는 로그인한 사원번호와 로그아웃 버튼이 공통으로 표시됩니다.

## 2. 데이터 흐름 (입력 → 처리 → 출력)

**질문-답변 흐름**
- 입력: 사용자가 Q&A 화면에 자연어 질문 입력
- 처리: 서버(API)가 질문을 임베딩(의미를 숫자로 변환)으로 바꾼 뒤, DB에 저장된 문서 임베딩과 비교해 의미가 가장 가까운 문서를 찾음(벡터 유사도 검색) → 찾은 문서를 AI 모델에 함께 전달 → AI가 그 문서 내용만으로 답변 생성
- 출력: 화면에 답변과 근거 문서(출처) 표시. 의미가 충분히 가까운 문서를 하나도 못 찾으면 AI는 "위키에 등록된 정보가 없습니다"라고만 답하고 추측하지 않음

**신규 문서 등록 흐름**
- 입력: 사용자가 카테고리별 탐색 화면에서 "새 문서 만들기" 클릭 → 카테고리 선택, 제목·본문 입력 후 등록
- 처리: 서버가 새 문서를 저장하고 임베딩을 생성 → 등록자 사원번호·시각을 "최초 등록"으로 로그에 남김
- 출력: 새로 등록한 문서의 상세 화면으로 이동, 카테고리별 탐색 목록과 Q&A 검색 대상에 바로 포함됨

**문서 수정 흐름**
- 입력: 사용자가 문서 상세 화면에서 내용 수정 후 저장
- 처리: 서버가 기존 내용을 로그 테이블에 백업 저장 → 문서 내용을 새 내용으로 갱신 (수정자 사원번호·시각 함께 기록)
- 출력: 화면에는 바로 최신 내용이 보이고, "수정 로그 보기"를 펼치면 방금 기록된 이력이 추가되어 있음

**되돌리기 흐름**
- 입력: 사용자가 로그에서 이전 버전 선택 후 "되돌리기" 클릭
- 처리: 서버가 그 이전 버전 내용을 현재 문서에 다시 저장하고, 이 되돌리기 자체도 새 로그로 기록
- 출력: 화면에 되돌린 버전이 최신 내용으로 표시

**로그인 흐름**
- 입력: 사원번호 + 공통 비밀번호
- 처리: 서버가 화이트리스트 목록에 그 사원번호가 있는지, 비밀번호가 맞는지 확인
- 출력: 통과하면 서버가 로그인 상태를 기억하는 세션(쿠키)을 발급 — 이후 모든 요청에 이 세션이 함께 전달되어 "누가 접속했는지" 서버가 알 수 있음(이 사원번호가 수정 기록에도 그대로 남음). 화이트리스트에 관리자로 표시된 사원번호면 세션에도 관리자 여부가 함께 저장됨. 실패하면 오류 메시지

**로그아웃 · 세션 만료 흐름**
- 입력: 사용자가 로그아웃 클릭, 또는 일정 시간 활동 없음
- 처리: 서버가 세션을 무효화(쿠키 삭제)
- 출력: 로그인 화면으로 돌아가며, 이후에는 다시 로그인해야 이용 가능

**관리자 화면 접근 제어**
- 입력: 사용자가 설정 탭 접속 시도
- 처리: 서버가 요청에 담긴 세션의 관리자 여부를 확인
- 출력: 관리자면 화면 표시, 아니면 설정 탭 자체가 보이지 않음

## 3. 기술 선택

**기본: Next.js** — PRD·CLAUDE.md에서 고정한 대로, 화면과 서버 코드(API)를 한 프로젝트에서 함께 만들고, Part 5에서 Vercel로 그대로 배포합니다.

이 위에 아래 3가지가 추가로 필요합니다. 모두 "왜 필요한지"를 쉽게 풀어 설명합니다.

| 추가 기술 | 쉬운 설명 1줄 |
|---|---|
| **데이터베이스 (Supabase Postgres)** | 문서·카테고리·수정 로그·사원번호 목록(관리자 여부 포함)을 저장해두는 "엑셀 표" 같은 저장 공간입니다. `pgvector` 확장을 사용해 의미 검색용 임베딩 값도 문서와 함께 여기 저장합니다. `.env`의 `SUPABASE_ACCESS_TOKEN`으로 Supabase CLI를 통해 관리합니다(CLAUDE.md 규칙). |
| **AI 모델 API (예: OpenAI 또는 Anthropic)** | 질문을 이해하고 문서 내용을 바탕으로 답을 만들어주는 "두뇌" 역할을 하는 외부 서비스입니다. 우리 서버가 이 서비스에 "이 문서들 참고해서 답해줘"라고 요청만 보내면 됩니다. 문서·질문을 의미 벡터(임베딩)로 바꿔 비슷한 내용을 찾는 데도 같은 서비스를 사용합니다. |
| **Prisma (DB 접근 도구)** | 우리 코드가 데이터베이스에 "문서 하나 가져와줘", "이 내용으로 저장해줘" 같은 명령을 쉬운 문법으로 내릴 수 있게 도와주는 도구입니다. 없어도 되지만 있으면 실수(오타로 인한 오류 등)가 줄어듭니다. |

---

이 설계로 진행해도 될까요, 아니면 화면 구성이나 기술 선택에서 바꾸고 싶은 부분이 있나요?
