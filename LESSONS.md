# 치과위키를 만들며 겪고 배운 것들

이 프로젝트(사내 위키 + LLM Q&A + 트리형 노트)를 만들면서 **실제로 부딪히고, 틀리고, 되돌린 것들**을 정리한 문서입니다.
저장소의 커밋 24개·마이그레이션 18개·코드 주석을 전부 훑어 근거를 확인하고 썼습니다.

## 이 문서를 읽는 법

- **§1**은 가장 비쌌던 다섯 사건입니다. 시간이 없으면 여기만 읽어도 됩니다.
- **§2~§8**은 분야별 교훈입니다. 각 항목은 `무슨 일이 있었나 → 왜 그런가(공부 포인트) → 어떻게 했나 → 남는 원칙` 순서입니다.
- **§9**는 조사하다 발견한, **지금도 저장소에 남아 있는 문제들**입니다. 전부 직접 확인했습니다.
- **§10**은 다음 프로젝트에 그대로 쓸 체크리스트, **§11**은 용어 사전입니다.
- 각 항목 끝의 `📁 파일:줄`은 실제 코드 위치입니다. 문서만 읽지 말고 그 파일을 같이 열어보세요.

> 공부 순서 추천: §1(사건) → §11(용어) → 관심 분야(§2~§8) → §9(남은 숙제를 직접 고쳐보기)

---

## §1. 가장 비쌌던 다섯 사건

### 사건 1. 다 만들고 배포한 기능을 2시간 반 만에 통째로 버린 날

"서버의 폴더·파일을 자유롭게 올리고 받는 기능"이라는 요청을 받아 **파일함(file-server)** 을 만들었습니다.
폴더별 권한, 서명 URL 업로드/다운로드, 재업로드, 30일 휴지통, Cron 자동 삭제까지 갖춰 API 시나리오 35개를 검증하고 배포까지 마쳤습니다(커밋 `ea11afc`, 28개 파일 2,512줄).

배포 후 사용자가 실제로 써 보자 진짜 요구가 드러났습니다 — 필요한 것은 파일 보관함이 아니라 **옵시디언 같은 트리형 메모장**이었습니다.
결정적 증거는 말이 아니라 **사용 흔적**이었습니다:

> "실제로 '치주과 인수인계' 폴더에 스크린샷을 올려봤지만 **화면에서 바로 볼 수 없어 휴지통으로 옮긴** 사용 흔적이 남아 있다"
> — `docs/01-plan/features/notes.plan.md:27`

커밋 시각으로 `ea11afc`(11:30) → `a0c83d6`(13:58). 배포 2시간 반 만에 기능 전체를 교체했습니다.

**왜 이런 일이 생기나** — 소프트웨어에서 가장 비싼 실수는 버그가 아니라 *"제대로 만든, 틀린 것"*(building the wrong thing right)입니다.
사용자는 보통 **해결책의 이름**("파일을 올리게 해주세요")을 말하지, **문제**("인수인계 자료를 사진과 함께 정리해 보고 싶다")를 말하지 않습니다.

**어떻게 수습했나** — 버릴 것과 재사용할 것에 선을 그었습니다.
화면(컴포넌트 4개)과 데이터 모델(테이블 3개 → `nodes` 트리 1개)은 버리고,
검증이 끝난 기반 계층 — 낙관적 잠금·불변 로그 트리거·`withSession` 인증·Storage 서명 URL·휴지통 Cron — 은 그대로 가져다 썼습니다.
사용자가 만든 데이터는 **같은 id를 유지한 채** 새 구조로 백필했습니다.
그리고 새 계획 문서에 `supersedes: file-server`를 명시해, 폐기된 설계를 나중에 현행으로 오해하지 않게 했습니다.

**남는 원칙**
- 요구사항은 **명사(무엇을 만들까)** 가 아니라 **상황(어떤 순간에 무엇이 불편했나)** 으로 물어라. 벤치마크 대상("옵시디언 같은")을 물어보는 것만으로도 방향이 달라진다.
- 말보다 **행동**이 정확하다. 사용자가 뭔가를 올렸다가 지운 흔적이 인터뷰보다 강한 신호였다.
- 계층이 분리돼 있으면 기능을 통째로 갈아엎어도 기반은 살아남는다. **재사용 가능한 것은 UI가 아니라 인프라**다.
- 방향이 틀렸다면 아까워하지 말고 빨리 버려라. 단, **이미 배포된 기능의 사용자 데이터를 깨뜨리지 않는 마이그레이션 경로**가 전제 조건이다.

📁 `docs/01-plan/features/notes.plan.md:8,19,24-49` · `supabase/migrations/20260904040000_notes_tree.sql:1-12`

---

### 사건 2. 검증이 버그를 스스로 가린 날 — `window.prompt()`

폴더·노트 생성을 브라우저 기본 다이얼로그 `window.prompt()`로 빠르게 구현했습니다. 로컬에서는 잘 됐습니다.

그런데 **실제 배포 환경(병원 업무용 브라우저)에서 `prompt()`가 "prompt() is not supported" 예외를 던졌습니다.**
예외가 클릭 핸들러 안에서 터지면서 폴더·노트 생성이 **아무 메시지 없이 조용히 실패**했습니다. 사용자 눈에는 그냥 "버튼이 안 눌린다"였습니다.

더 심각한 건 그 직전 커밋의 검증 기록이었습니다:

> "브라우저에서 폴더 생성→노트 작성→이미지 삽입→저장→렌더링→이름변경→삭제→복구→검색까지 **실제 조작 확인**" — `a0c83d6`

분명히 "실제로 눌러봤다"고 했는데 왜 못 잡았을까요? 수정 커밋의 검증 문구가 답을 드러냅니다:

> "로컬 dev에서 **네이티브 다이얼로그를 흉내내지 않고(이번엔 진짜 prompt/confirm 없이)** … 실제로 확인" — `bb02e61`

즉 앞선 검증은 테스트 편의를 위해 `window.prompt`를 가짜로 바꿔치기한 뒤 통과시켰던 것입니다.
**실패하는 바로 그 지점을 검증에서 스스로 제거한 셈**입니다.

**왜 그런가** — `prompt/alert/confirm`은 브라우저 UI 스레드를 통째로 멈추는 동기 다이얼로그라, 최신 브라우저·관리형 브라우저·기업 정책 환경·크로스 오리진 iframe에서 무시되거나 예외를 던지도록 제한되고 있습니다. "내 크롬에서는 되는데 사용자 환경에서는 안 되는" 대표적 API입니다.

**어떻게 고쳤나** — 네이티브 다이얼로그를 걷어내고 **화면 안 UI**로 바꿨습니다.
생성은 트리 안 인라인 입력 폼으로, 삭제 확인은 한 번 누르면 "정말 삭제할까요?"로 바뀌는 **2단계 버튼**으로.
그리고 되돌림을 막으려고 컴포넌트 상단에 이유를 주석으로 남겼습니다.

**남는 원칙**
- 테스트에서 무언가를 스텁/모킹으로 대체할 때는 반드시 물어라: **"지금 내가 검증하려는 위험 자체를 제거하고 있지 않은가?"**
  스텁이 정하는 것은 "무엇을 편하게 테스트할까"가 아니라 **"무엇을 검증하지 않기로 할까"** 다.
- 사용자 동작의 실패는 절대 조용해서는 안 된다. 예외를 삼키는 경로가 있으면 화면에 드러내라.
- 한 번 "이 API는 쓰지 않는다"고 정했으면 **코드베이스 전체**에 적용하라.
  (→ §9에 적었듯, 실제로 `SettingsWhitelist.tsx`에는 아직 `window.confirm`이 남아 있습니다.)

📁 `components/notes/NoteTree.tsx:18-22,47-65` · `components/notes/NoteEditor.tsx:26-31,54-55`

---

### 사건 3. 우회책을 넣었다가 51분 만에 철회한 날 — 감사 로그의 외래키

"등록되지 않은 사원번호로는 접속할 수 없다"(PRD 7번)를 지키려면 퇴사자를 화이트리스트에서 지울 수 있어야 했습니다.
그런데 초기 스키마가 이렇게 되어 있었습니다:

```sql
created_by text not null references employee_whitelist(employee_id)
edited_by  text not null references employee_whitelist(employee_id)
```

외래키 때문에 **문서를 한 번이라도 작성·수정한 사원번호는 삭제 자체가 불가능**했습니다.

> "실제로 00001은 문서 4건·로그 7건 때문에 삭제가 막혀 있었고, 결과적으로 퇴사자 차단 수단이 없는 상태였다"
> — `20260903062552_whitelist_is_active.sql:6-7`

**첫 대응(06:25:52)** 은 우회였습니다 — `is_active` 컬럼을 추가해 "삭제 대신 비활성화".
하지만 이건 증상 치료였고, 로그인·매 요청 세션 확인·설정 화면 **세 곳에 `is_active` 분기를 새로 뿌려야 하는** 부담까지 생겼습니다.

**55분 뒤(07:21:11)** 방향을 뒤집었습니다:

> "감사 로그는 과거의 기록이다. 행위자는 **그 시점의 값으로 남겨두면 충분**하고, 살아있는 계정 테이블을 참조할 이유가 없다"

외래키 두 개를 떼고 컬럼의 성격을 **스냅샷**으로 재정의하자 계정을 자유롭게 지울 수 있게 됐고, 같은 마이그레이션 마지막 줄에서 `is_active` 컬럼 자체를 제거했습니다.

**왜 그런가** — 데이터에는 성격이 다른 두 종류가 있습니다.
**현재 상태를 나타내는 데이터**(마스터 데이터)와 **과거에 일어난 사실**(트랜잭션 데이터).
주문서에 상품명과 가격을 그대로 복사해 넣는 이유와 같습니다 — 상품 가격이 나중에 바뀌어도 과거 주문서 금액은 그때 그대로여야 하니까요.
감사 로그의 '누가'도 정확히 같은 성격입니다.

**잃은 것도 정직하게 적었습니다**
> "DB가 'edited_by에는 실존하는 사원번호만 들어간다'를 더 이상 보장하지 않는다. 다만 이 값은 항상 로그인 시 화이트리스트로 검증된 세션에서만 오므로 앱 차원의 정합성은 유지된다"
> "퇴사자 사원번호를 회수해 새 직원에게 재부여하면 옛 기록이 새 직원 것으로 보일 수 있다. **사번을 재사용하지 않는 운영을 전제한다**"

**남는 원칙**
- 감사 로그·주문 이력처럼 시점을 보존해야 하는 데이터는 **참조(FK)가 아니라 스냅샷**으로 저장하라.
- 요건이 막혔을 때 우회 컬럼을 추가하기 전에 **"무엇이 진짜 원인인가"** 를 한 번 더 물어라. 우회책은 코드 여러 곳에 분기를 퍼뜨리고, 되돌리는 비용이 원래 문제를 푸는 비용보다 커진다.
- 잘못된 방향을 알아챘다면 **한 시간 안에 되돌리는 것이 가장 싸다.**
- 설계를 바꿀 때는 **이 선택으로 잃는 것**을 반드시 함께 문서화하라.

📁 `20260903062552_whitelist_is_active.sql` · `20260903072111_author_snapshot_allow_delete.sql`

---

### 사건 4. '사랑니'를 틀니 문서가 이긴 날 — 임베딩만 믿은 대가

Q&A는 문서 하나당 임베딩 하나를 만들고 유사도 상위 N개를 문턱값 0.5로 거르는 가장 단순한 구조였습니다.
짧은 예시 문서 2건만 있을 때는 잘 되는 것처럼 보였습니다.

실제 수가 문서 7건(1,600~2,600자)을 넣자 무너졌습니다:

> "발치 수가 문서(약 2,000자)에 **'사랑니 빼는 거 얼마예요?'가 그대로 적혀 있는데도** 같은 질문의 유사도가 **0.27**에 그쳐 문턱값 0.5를 넘지 못했다. 반면 281자짜리 예시 문서는 0.58을 받았다. **길이가 원인**이라는 뜻이다."
> "더 나쁜 것은 순위까지 틀렸다는 점이다 — '사랑니'에 **틀니 문서(0.2971)가 발치 문서(0.2665)보다 위로** 올라왔다."

**1차 수정: 청킹** — 검색 단위를 문서에서 `## ` 절 단위 조각으로 내렸습니다. 그런데도 8건 중 2건이 여전히 틀렸습니다:

```
"사랑니 빼는 거 얼마예요?" → 틀니 0.2989 > 발치 0.2879  (오답)
"실란트 보험 되나요?"     → 틀니 0.2571 > 치석제거 0.2526 (오답)
```

같은 질문을 **키워드로 찾으면 둘 다 1위를 정확히 맞혔습니다**(발치 0.6051, 치석제거 0.3411).
임베딩 모델이 한국어 고유명사(사랑니, 실란트)의 변별력을 충분히 갖고 있지 않았던 것입니다.

**2차 수정: 하이브리드 검색**
```sql
0.6 * (1 - (c.embedding <=> query_embedding))              -- 의미 검색
+ 0.4 * least(ts_rank(c.content_tsv, q.tsq), 1.0)          -- 키워드 검색
```
결과: 실제 질문 10개 **전부 1위 정답**(이전 6/8).

**왜 그런가** — 임베딩은 텍스트 길이와 무관하게 **고정 차원 벡터 하나**로 압축됩니다.
긴 문서를 통째로 넣으면 모델이 사실상 글 전체의 *평균적 의미*를 잡기 때문에, 문서 안 특정 문장("사랑니")의 신호가 다른 내용에 희석됩니다.
그리고 벡터 검색과 키워드 검색은 **실패하는 지점이 다릅니다** — 전자는 표현이 달라도 찾아주지만 정확한 낱말을 놓치고, 후자는 낱말은 잡지만 의역에 약합니다.

**남는 원칙**
- RAG가 엉뚱한 답을 내면 **프롬프트부터 만지지 마라.** 먼저 "검색이 정답 문서를 가져왔는가"를 **유사도 숫자로 측정**하라. 원인은 대개 생성이 아니라 검색에 있다.
- 검색 단위는 **사람이 답을 찾을 때 읽는 최소 단위**(절/문단)에 맞춰라. 검색 단위와 화면 표시 단위는 분리해도 된다(조각으로 찾고 문서로 보여주기).
- 하이브리드가 실무의 기본값이다.
- **점수 산식을 바꿨으면 문턱값도 반드시 다시 측정하라.** 산식이 바뀌는 순간 옛 문턱값의 의미는 사라진다(실제로 0.5 → 0.2로 다시 쟀습니다).

📁 `20260903232221_document_chunks.sql:1-18` · `20260904000701_hybrid_search.sql:1-26,45-57` · `lib/chunks.ts`

---

### 사건 5. 버튼마다 400~900ms — '즉시 반영'의 청구서

사건 3의 결과로, 세션 토큰에는 **사원번호만** 담고 권한은 매 요청 DB에서 다시 읽기로 했습니다.
계정 삭제·관리자 강등이 **즉시** 반영되는 좋은 설계였습니다.

그런데 실사용에서 버튼 클릭마다 눈에 띄게 느렸습니다. 실제로 재봤습니다:

```
/api/notes/tree 연속 호출
1번째: 9,936ms  (개발 서버 최초 컴파일 포함)
2번째:   394ms
3번째:   421ms
4번째:   911ms
5번째:   727ms
```

원인은 명확했습니다 — **버튼 하나 누를 때마다 Supabase 왕복이 2회**였습니다.
① "이 사원번호가 아직 화이트리스트에 있나?" ② 실제로 요청한 작업.

**해결** — 화이트리스트 조회 결과를 서버 메모리에 **30초만** 캐싱했습니다. 캐시 적용 후 반복 호출은 ~200ms로 안정화됐습니다.

설계 디테일 세 가지가 중요합니다:
1. **계정 없음도 캐싱한다** — 그러지 않으면 삭제된 계정이 매번 DB를 때린다(캐시 관통 방지).
2. **DB 오류는 캐싱하지 않는다** — 일시적 장애를 30초짜리 강제 로그아웃으로 굳히지 않기 위해.
3. React `cache()`(같은 요청 안 중복 제거)와 이 TTL 캐시(요청 간 공유)는 **층이 다르며 둘 다 필요하다.**

**대가를 명시적으로 기록했습니다**
> "화이트리스트 삭제·강등 반영이 '즉시'에서 '**최대 30초 지연**'으로 바뀌는 트레이드오프를 사용자와 합의하고 적용했다" — `lib/auth.ts:25-27`

**남는 원칙**
- 보안 요구("즉시 반영")는 대개 성능 비용을 동반한다. **"즉시"가 정말 필요한 시간 단위인지 되물어라.**
  퇴사자 차단이 0초여야 하는지 30초여도 되는지는 **보안 문제가 아니라 업무 문제**이고, 그 답은 개발자가 아니라 사용자가 정한다.
- 느리다는 느낌이 들면 **추측하지 말고 재라.** 숫자가 있어야 무엇을 고칠지 정할 수 있다.
- 트레이드오프를 감수했다면 **무엇을 얻고 무엇을 잃었는지를 숫자와 함께** 주석·커밋 메시지에 남겨라.

📁 `lib/auth.ts:20-64` · 커밋 `374fc14`

---

## §2. 데이터베이스 — 마이그레이션 18개가 가르쳐 준 것

### 2.1 여러 테이블에 나눠 쓰는 작업은 DB 함수 하나로 묶어라

문서를 저장할 때 `documents`에 본문을 쓰고 `document_logs`에 수정 로그를 남겨야 했습니다.
처음에는 API가 두 번 요청했는데, ①이 성공하고 ②가 실패하면 **로그 없는 문서**가 남았습니다.

> "기존 코드는 이때 500을 돌려주긴 했지만 **문서는 이미 저장된 상태로 남았다**"

사용자는 "실패했다"고 보는데 실제로는 절반이 반영된 최악의 상태입니다.

**공부 포인트** — PostgreSQL에서 **함수 호출 하나 = 트랜잭션 하나**입니다. 함수 안에 INSERT 세 개를 넣으면 셋 다 되거나 셋 다 안 됩니다.
반면 애플리케이션에서 HTTP 요청을 두 번 보내면 그 둘은 서로 다른 트랜잭션이라 하나만 성공할 수 있습니다.

**한 가지 더** — 임베딩 생성(OpenAI 호출)은 DB 안에서 할 수 없습니다.
그래서 **비싼 외부 계산은 앱에서 미리 끝내고 결과값만 인자로 넘기고**, 원자성이 필요한 쓰기만 DB 함수에 맡기는 분업이 정석입니다.

**원칙** — 한 번의 사용자 동작이 여러 테이블을 바꾼다면, 그 경계를 애플리케이션이 아니라 **DB 트랜잭션 안**에 두어라.

📁 `20260903131032_document_write_functions.sql` · `lib/document-write.ts`

### 2.2 불변 로그는 트리거로 강제하되, 마이그레이션용 출구를 남겨라

"기록된 로그는 수정·삭제할 수 없다"(PRD 5번②)를 DB 트리거로 강제했습니다.
그러자 정반대 문제가 생겼습니다 — 스키마를 확장하며 기존 로그의 빈 칸을 채워야 하는데(백필), **트리거가 그 UPDATE도 막았습니다.**

해결은 그 마이그레이션 안에서만 잠깐 끄고 즉시 되돌리는 것:
```sql
alter table document_logs disable trigger document_logs_no_update;
update document_logs set new_title = ...;
alter table document_logs enable trigger document_logs_no_update;
```
정당화도 주석에 남겼습니다:
> "여기서 하는 일은 로그 내용의 변경이 아니라, 방금 추가한 빈 칸을 그 시점의 사실대로 채우는 **스키마 이관**이다. 이 마이그레이션 안에서만 끄고 즉시 되돌린다."

**공부 포인트** — PostgreSQL은 DDL도 트랜잭션 안에서 롤백되므로, 마이그레이션 전체가 한 트랜잭션이면 도중에 실패해도 **트리거가 꺼진 채 방치되지 않습니다.** 단, "이 마이그레이션이 한 트랜잭션으로 도는가"를 먼저 확인해야 이 안전장치가 성립합니다.
그리고 **BEFORE DELETE 트리거는 TRUNCATE에 발동하지 않습니다** — 진짜 불변으로 만들려면 트리거 + 권한 회수(§3.1)를 함께 써야 합니다.

**원칙** — 지켜야 할 불변식은 사람의 규율이 아니라 DB가 강제하게 하라. 다만 강제 장치를 만들 때는 **정당한 예외를 어떻게 통과시킬지**를 함께 설계하라. 출구 없는 강제는 결국 마이그레이션 앞에서 무너진다.

📁 `20260828065522_init_schema.sql:52-65` · `20260903130809_log_title_history.sql:20-37`

### 2.3 낙관적 잠금 — 조용한 덮어쓰기를 막는다

행 잠금(`for update`)은 이미 걸려 있어서 데이터가 뒤엉키지는 않았습니다. 문제는 다른 데 있었습니다:

> "A가 문서를 열어 고치는 동안 B도 같은 문서를 열어 고치면, B가 저장하는 순간 A의 수정 내용이 **아무 경고 없이 사라진다.** 로그에는 남아 되돌릴 수는 있지만, **B는 자기가 A의 작업을 덮어썼다는 사실 자체를 모른다.**"

`version` 정수 컬럼을 두고, 클라이언트가 화면에 띄운 시점의 버전을 함께 보내고, 다르면 `VERSION_CONFLICT`를 던져 **409**로 변환합니다.

**타임스탬프를 안 쓴 이유도 주석에 있습니다**
> "클라이언트가 JS Date로 한 번 거쳐 오는 과정에서 Postgres의 마이크로초 정밀도가 밀리초로 잘리는 등 미묘하게 어긋날 여지가 있다. **정수 비교는 그런 여지가 없다.**"

**원칙** — 충돌을 막는 것보다 중요한 것은 **충돌이 일어났음을 사용자에게 알리는 것**이다. 버전 식별자는 타임스탬프보다 단조 증가 정수를 써라.

📁 `20260904010000_optimistic_concurrency.sql`

### 2.4 함수는 이름이 아니라 **시그니처**로 식별된다

`create or replace function`에 인자를 하나 추가했더니 교체가 아니라 **새 함수가 하나 더 생겼습니다**(오버로드).

> "그 결과 같은 이름의 함수가 두 개가 되어 호출이 모호해지고, **옛 함수가 불리면 조각이 갱신되지 않은 채 문서만 바뀌어** 검색 결과가 어긋난다"

인자가 같아도 실패하는 경우도 있었습니다 — **반환 타입이 바뀌면** `create or replace`가 거부됩니다.

**원칙** — 시그니처를 바꿀 때는 항상 `drop function if exists <이름>(<정확한 인자 타입>)`으로 **옛 시그니처를 먼저 지워라.**

📁 `20260904000229_drop_old_write_functions.sql` · `20260904010000:26-34`

### 2.5 CHECK 제약을 좁히며 데이터도 바꾸는 순서는 하나뿐

`'image'` → `'attachment'` 이름 변경에서 **두 번 연속 실패**했습니다. 어느 쪽을 먼저 해도 막히는 교착처럼 보였습니다:

> "데이터를 먼저 바꾸면(옛 제약이 아직 살아있는 상태) 'attachment'가 옛 허용값에 없어서 실패하고, 반대로 제약만 먼저 좁히면 아직 'image'인 기존 행이 새 허용값에 없어서 실패한다"

정답은 **3단계로 쪼개는 것**입니다:
```
① 옛 제약 전부 DROP  →  ② 데이터 UPDATE  →  ③ 새 제약 ADD
```

**공부 포인트** — CHECK 제약은 검사 시점이 두 곳입니다: **추가할 때 기존 전체 행을 한 번**, 그 뒤로는 INSERT/UPDATE마다. 그래서 '좁히는' 변경은 항상 기존 데이터와 충돌할 수 있습니다.
(대안: `ADD CONSTRAINT ... NOT VALID` 후 나중에 `VALIDATE CONSTRAINT` — 대용량 테이블의 락 시간을 줄일 때 씁니다.)

📁 `20260904060000_note_scoped_attachments.sql:17-44`

### 2.6 트리는 자기참조 테이블 + 재귀 CTE로 — 방향이 반대인 두 연산

폴더·노트·첨부를 `nodes` 한 테이블에 `type` 컬럼으로 구분하고 `parent_id`가 같은 테이블을 참조합니다.

- **휴지통 이동/복구는 위 → 아래**: `where parent_id = ?`는 자식 한 단계만 처리합니다. 손자·증손자가 살아남아 **유령 노드**가 됩니다. → `with recursive`로 하위 전체 id를 모은 뒤 한 번에 갱신.
- **완전 삭제는 아래 → 위**: 부모를 먼저 지우면 자식의 외래키가 깨집니다. → 자식 없는 것(리프)만 골라 지우는 것을 반복하는 루프.

**공부 포인트** — `parent_id`로 부모를 가리키는 방식을 **인접 리스트(adjacency list)** 모델이라 합니다. 구조는 단순하지만 조상/자손 전체를 구하려면 재귀가 필요합니다.
주의할 점은 **순환**입니다 — 폴더를 자기 자손 밑으로 옮길 수 있게 만들면 재귀가 끝나지 않습니다(이 프로젝트는 아직 '옮기기' 기능이 없어 안전).

📁 `20260904050000_notes_write_functions.sql`

### 2.7 파생 데이터는 원본과 같은 트랜잭션에서, 그리고 재생성 스크립트를 준비하라

검색용 조각(청크)은 본문에서 계산되는 **파생 데이터**입니다.
본문만 바뀌고 조각이 옛것으로 남으면 **직원이 검색으로 옛 수가(금액)를 보게 되고**, 그건 곧 잘못된 청구입니다.

세 가지로 정리했습니다:
1. 본문 갱신 + 로그 기록 + 조각 재생성을 **한 트랜잭션**에.
2. `on delete cascade`로 파생임을 스키마에 못박기 — *"조각은 원본이 아니라 검색용 파생 데이터이므로 로그처럼 보존할 필요가 없다."*
3. `scripts/rebuild-chunks.mjs`로 **전량 재생성 경로**를 1급 도구로 준비.

**원칙** — 데이터를 **원본(source of truth)** 과 **파생(derived)** 으로 나눠 생각하는 습관을 들여라. 파생은 언제든 다시 만들 수 있어야 하고, 그게 가능하면 장애 복구가 훨씬 쉬워진다.

📁 `20260903235546_write_functions_with_chunks.sql` · `scripts/rebuild-chunks.mjs`

---

## §3. 보안 — 방어선이 하나뿐일 때

### 3.1 RLS를 켜는 것만으로는 부족하다

초기 스키마에는 `enable row level security`가 **한 줄도 없었고**, Supabase가 기본으로 `anon` 롤에 주는 테이블 권한이 그대로 살아 있었습니다.
`anon` 키는 **브라우저에 노출되도록 설계된 공개 키**입니다. 즉 공개 키만으로 사원번호 화이트리스트를 통째로 읽을 수 있었습니다.

더 미묘한 문제는 **TRUNCATE**였습니다 — RLS는 TRUNCATE에 적용되지 않고, 불변 로그 트리거(BEFORE DELETE)도 TRUNCATE에는 발동하지 않습니다. RLS만 켜면 "로그는 수정·삭제할 수 없다"가 **TRUNCATE 한 방으로 우회**됩니다.

**3중 방어**
```sql
alter table employee_whitelist enable row level security;          -- ① 정책 0개 = 전면 차단
revoke all on table employee_whitelist from anon, authenticated;   -- ② TRUNCATE까지 차단
alter default privileges in schema public
  revoke all on tables from anon, authenticated;                   -- ③ 미래의 테이블까지
```

**공부 포인트** — PostgreSQL 권한은 **두 겹**입니다. 바깥층 `GRANT/REVOKE`("이 롤이 이 테이블을 건드릴 수 있나")와 안쪽층 `RLS`("그중 어떤 행이 보이나"). **두 층은 서로를 대체하지 않습니다.**

**원칙** — 보안 설정은 "지금 있는 것을 막았나"보다 **"앞으로 생길 것도 막히나"** 로 검증하라.

📁 `20260903061446_enable_rls.sql:20-31`

### 3.2 아이디가 공개값이면 방어선은 사실상 하나다

로그인은 "사원번호(화이트리스트) + 전 직원 공통 비밀번호"입니다. 소규모 병원 현장에 맞춘 선택이지만, 마이그레이션 주석이 위험을 직접 적어뒀습니다:

> "사원번호는 비밀이 아니다 — 사내 메일 주소나 명찰에서 그대로 드러난다. 즉 **실질적인 방어선은 공통 비밀번호 하나뿐**인데, 시도 횟수 제한이 없으면 인터넷에서 무제한 속도로 계속 찍어볼 수 있었다"

**원칙** — 인증 모델을 단순화해 편의를 얻었다면, **그 단순화가 어떤 방어선을 지웠는지 명시적으로 적고** 남은 방어선을 여러 겹으로 보강하라. 먼저 물어야 할 질문은 **"아이디가 공개값인가?"** 다.

### 3.3 로그인 실패는 항상 같은 메시지로 + `timingSafeEqual`

실패 경우가 두 가지(비밀번호 틀림 / 미등록 사번)인데 구분해 알려주면, 공격자는 아무 비밀번호나 넣고 **응답 메시지만 보고 유효 계정 목록을 캐낼 수 있습니다**(사용자 열거).

비밀번호 비교도 `===`를 쓰면 안 됩니다:
```ts
return inputBuffer.length === expectedBuffer.length &&
       timingSafeEqual(inputBuffer, expectedBuffer);
```

**공부 포인트** — **타이밍 공격**: 문자열 비교는 불일치를 만나면 즉시 멈추므로, 앞 글자가 맞을수록 미세하게 더 오래 걸립니다. 이 차이를 수만 번 측정하면 한 글자씩 알아낼 수 있습니다. `timingSafeEqual`은 버퍼 전체를 끝까지 훑어 시간을 입력과 무관하게 만듭니다(길이가 다르면 예외를 던지므로 길이 검사가 먼저 와야 합니다).

**한 걸음 더** — 정보 누출은 화면에서도 새어나갑니다. 로그인하지 않은 채 `/documents/<아무값>`에 접근하면 404가 아니라 `/login`으로 리다이렉트합니다. *"404를 주면 '그 문서는 없다'는 정보를 외부인에게 흘리는 셈"* 이기 때문입니다.

📁 `app/api/auth/login/route.ts:7-21,66-80` · `tests/api/auth.test.mts:77-82`

### 3.4 메모리에 두어도 되는 상태 vs 절대 안 되는 상태

이 프로젝트에는 겉보기에 모순되는 두 결정이 있습니다:
- 로그인 시도 횟수 → **메모리로 세면 안 됨**, DB 테이블로.
- 화이트리스트 조회 결과 → **메모리 Map에 30초 캐싱해도 됨**.

판단 기준은 속도가 아니라 **"사라졌을 때 어느 쪽으로 틀리는가"** 입니다.

| 상태 | 사라지면 | 판정 |
|---|---|---|
| 화이트리스트 캐시 | DB를 한 번 더 조회할 뿐 (느려지지만 안전) | 메모리 OK |
| 로그인 시도 카운터 | 카운트가 0으로 초기화 → **무제한 시도 가능** | 반드시 공유 저장소 |

> "Vercel은 서버리스라 요청마다 다른 인스턴스에서 처리될 수 있고 메모리도 초기화된다. 따라서 프로세스 메모리에 카운터를 두면 셀 수 없다"

**원칙** — **fail-safe(안전한 쪽으로 실패)** 하는 상태만 메모리에 둬라. 보안 카운터·분산 락·멱등성 키는 반드시 공유 저장소로.
그리고 보안 검사는 **fail-closed**로 설계하라 — 이 프로젝트는 rate limit 확인 자체가 실패하면 통과가 아니라 **503으로 막습니다**(*"확인 없이 통과시키면 제한이 무의미해진다"*).

📁 `20260903142230_login_rate_limit.sql:9-18` · `lib/auth.ts:29-31`

### 3.5 HMAC 서명 쿠키는 서버가 무효화할 수 없다

세션은 DB 없이 `base64url(payload).HMAC서명` 형태의 자기완결형 쿠키입니다. 빠르지만 결정적 한계가 있습니다 — **서버가 이미 발급한 개별 토큰을 취소할 방법이 없습니다.**
초기에는 계정을 지워도 최대 8시간 동안 계속 접속됐습니다.

**우회 방법** — 토큰에서 권한을 **아예 빼는 것**:
> "isAdmin은 여기에 두지 않는다 — 관리자 강등이 즉시 반영되지 않는 **낡은 값을 신뢰하게 되기 때문**이다"

즉 **"토큰은 이 사람이 누구인지만 증명하고, 무엇을 할 수 있는지는 매번 DB가 결정한다"** 로 역할을 쪼갰습니다.
이 규칙은 테스트로도 못 박혀 있습니다 — 토큰 본문을 디코드해 `assert.equal(body.includes("isAdmin"), false)`.

**원칙** — 자기완결형 토큰(JWT·서명 쿠키)에는 **"바뀌면 즉시 반영돼야 하는 값"을 넣지 마라.** 넣는 순간 그 값의 갱신 지연은 토큰 수명과 같아진다.

📁 `lib/session.ts:6-11` · `lib/auth.ts:12-18` · `tests/unit/session.test.mts:35-41`

### 3.6 쿠키 옵션 네 개는 각각 다른 공격을 막는다

```ts
{ httpOnly: true, secure: NODE_ENV === "production", sameSite: "lax", path: "/" }
// maxAge 없음 → 브라우저를 완전히 닫으면 쿠키도 사라진다(세션 쿠키)
```

| 옵션 | 막는 것 |
|---|---|
| `httpOnly` | XSS로 `document.cookie`를 읽어 세션 탈취 |
| `secure` | HTTP 평문 전송 중 도청 |
| `sameSite: lax` | CSRF (외부 사이트가 보낸 위조 요청에 쿠키가 딸려가는 것) |
| 만료 설정 | 기기 공유·방치 |

만료는 **두 군데**에 걸었습니다 — 쿠키 수명(브라우저가 지키는 약속)과 토큰 안의 `exp`(서버가 검증하는 진짜 상한선).
크롬의 '이전 탭 복원'이 켜져 있으면 세션 쿠키가 살아남을 수 있으므로 서버 쪽 `exp`가 여전히 필요합니다.

**원칙** — 옵션 하나씩 **"이걸 빼면 무엇이 뚫리는가"** 를 답할 수 있어야 하고, 그 답을 주석에 남겨야 나중에 누가 편의를 이유로 지우지 못한다.

📁 `app/api/auth/login/route.ts:94-102` · `app/api/auth/logout/route.ts:4-7`

### 3.7 XSS는 "걸러내기"보다 "경로를 안 만들기"

노트는 전 스탭이 함께 편집하는 마크다운입니다 — 전형적인 **저장형 XSS** 위험 지점입니다.

`react-markdown`은 마크다운을 문자열 HTML이 아니라 **React 엘리먼트로 직접 변환**하고, 원문 속 HTML 태그는 기본적으로 렌더링하지 않습니다.
`rehype-raw` 같은 HTML 패스스루를 켜지 않는 한 `<script>`가 실행될 **경로 자체가 없습니다.** 그래서 sanitize 라이브러리를 아예 추가하지 않았습니다.

유일한 예외인 PDF 인라인 미리보기(iframe)는 **허용 목록**으로 좁혔습니다:
```ts
const ATTACHMENT_LINK = /^\/api\/notes\/[0-9a-f-]{36}\/content$/i;
```

**원칙** — 안전한 기본값을 가진 라이브러리를 고르고, **그 기본값을 끄지 않는 것 자체가 설계 결정**임을 문서로 남겨라. 그래야 다음 사람이 "HTML도 되게 해달라"는 요청에 무심코 `rehype-raw`를 켜지 않는다.

📁 `components/notes/MarkdownView.tsx:6-12,20-48`

### 3.8 비공개 버킷을 유지한 채 `<img src>`에 바로 쓰는 법

`<img src>`는 브라우저가 직접 거는 요청이라 **Authorization 헤더를 붙일 수 없습니다.**
서명 URL을 마크다운 본문에 저장하는 방법도 안 됩니다 — 이 프로젝트의 서명 URL은 **60초짜리 단명 토큰**이라 곧 죽습니다.
이미지 바이트를 서버가 프록시로 흘려주면 서버리스 함수가 모든 이미지 트래픽을 통과하게 되어 느리고 비쌉니다.

**해법: 인증 라우트 + 307 리다이렉트**
```
마크다운에는 영구 경로만 저장 → /api/notes/{id}/content
그 라우트가 ① 로그인 확인 ② 노드가 유효한 첨부인지 확인 ③ 서명 URL을 그 자리에서 새로 발급해 307 리다이렉트
```
브라우저는 리다이렉트를 자동으로 따라가므로 `<img>`도 `<a>`도 `<iframe>`도 전부 동작하고, 버킷은 끝까지 비공개입니다.

**원칙** — URL을 **영구·안정적인 것**(내 도메인 경로)과 **단명·비밀인 것**(서명 URL)으로 분리하고, 저장·공유하는 것은 항상 앞쪽만.

📁 `app/api/notes/[id]/content/route.ts` · `lib/file-storage.ts:3-6`

### 3.9 업로드 검증은 서버에서 두 번, 경로에는 UUID만

`<input accept="image/*">`는 **파일 선택창의 필터일 뿐 강제력이 전혀 없습니다.** "모든 파일"로 바꾸면 통과하고, curl로 API를 직접 부르면 화면 검증은 존재하지도 않습니다.

그래서 서버 라우트 **두 곳**에서 같은 규칙을 검사합니다:
① `upload-url` 발급 시점(여기서 막아야 서명 URL이 아예 안 나감) ② `attachments` 확정 등록 시점(서명 URL을 받은 뒤 파일명을 바꿨을 수 있으므로).

스토리지 경로는 **UUID 두 조각만** 잇습니다 — 사용자가 올린 원래 파일명은 DB에만 저장하고 경로에는 절대 쓰지 않습니다.
`../` 같은 경로 조작을 *정제*하는 것보다 **애초에 사용자 입력을 경로에 안 쓰는 것**이 압도적으로 안전합니다.

또 하나: `/documents/x` 처럼 uuid가 아닌 값이 그대로 DB로 흘러가 **500 서버 오류**가 나던 문제를, 라우트 진입 시점의 `isUuid()` 검사로 404로 바꿨습니다.
> 사용자 오타가 5xx 알람을 울리기 시작하면 **알람 자체를 신뢰하지 않게 되고**, 그러면 진짜 장애를 놓칩니다.

📁 `lib/file-rules.ts` · `lib/uuid.ts` · `lib/file-storage.ts:8-12`

### 3.10 비밀값은 '어디에 두는가'보다 '어디에 안 두는가'

- **Production**: 앱 실행에 실제로 필요한 5개만 등록.
- `GITHUB_TOKEN`·`VERCEL_TOKEN`·`SUPABASE_ACCESS_TOKEN`·`SUPABASE_DB_PASSWORD`: 로컬 CLI 전용이라 **올리지 않음** — *"배포된 앱이 쓸 이유가 없는 값은 주지 않는다."*
- **Preview 환경변수는 일부러 비워둠** — 실험 중인 브랜치가 운영 DB에 붙는 것을 막기 위해. 접근 자체도 Vercel Authentication으로 잠금.

**공부 포인트** — Next.js에서 `NEXT_PUBLIC_` 접두사가 붙은 값은 **빌드 결과물에 그대로 박혀 브라우저로 나갑니다.** Supabase의 `service_role`(전권, 서버 전용)과 `anon`(브라우저 노출 전제)은 이름이 비슷해도 취급이 정반대입니다.

**원칙** — 값을 비워 두는 것도 정당한 설정이며, **그 이유를 문서에 남겨야** 다음 사람이 '빠진 값'으로 오해해 채워 넣지 않는다.

📁 `README.md:71-98` · `.env.example`

---

## §4. RAG 검색 — 프롬프트보다 먼저 숫자를 재라

### 4.1 조각은 문맥을 잃는다 — 제목을 붙여라

수가 문서 세 개가 `## 장애인 가산` 이라는 **거의 같은 절**을 나눠 갖고 있었습니다.
절만 떼어 놓으면 발치 얘긴지 근관치료 얘긴지 알 수 없어 엉뚱한 문서가 근거로 잡혔습니다.

**해결**: 모든 조각을 임베딩할 때 문서 제목을 앞에 붙입니다 — `` `${title}\n\n${piece}` ``.
반대로 **LLM에게 넘기는 근거는 문서 전체가 아니라 실제로 걸린 조각만** 넘깁니다.

**공부 포인트** — 이 기법을 **contextual chunking**이라 합니다.
그리고 LLM 쪽에는 *lost in the middle* 현상이 알려져 있습니다 — 긴 컨텍스트를 주면 가운데 정보를 잘 못 씁니다.
**검색 단계에는 문맥을 더하고, 생성 단계에는 문맥을 덜어내는 것**이 원칙입니다.

### 4.2 문턱값은 실측으로 정하고, 문턱값 하나만 믿지 마라

실제 질문 14개로 점수를 찍어보니 두 구간이 **겹쳤습니다**:
```
정답이 있어야 하는 질문 10개: 0.2172 ~ 0.4530
위키에 없는 질문 4개      : 최고 0.2713
```
어떤 값을 골라도 '정답인데 탈락' 또는 '무관한데 통과' 중 하나는 발생합니다.

그래서 문턱값을 0.2로 낮추고 **방어선을 두 겹**으로 만들었습니다:
> "1차: 이 문턱값이 명백히 무관한 문서를 거른다 / 2차: 근거가 넘어가도 프롬프트가 '문서에 없으면 추측하지 말라'고 지시한다"

**공부 포인트** — 검색 품질은 **재현율**(정답을 놓치지 않음)과 **정밀도**(쓸데없는 걸 안 가져옴)의 맞교환입니다.
RAG에서는 대개 재현율 쪽으로 기울여 놓는 편이 안전합니다 — **검색이 못 찾은 것은 LLM이 절대 복구할 수 없지만, 필요 없는 걸 가져온 것은 LLM이 무시할 수 있기** 때문입니다.

**원칙** — 마법의 숫자는 반드시 **근거 측정값과 함께** 주석으로 남겨라. 그래야 나중에 값을 만지는 사람이 재측정 없이 흔들지 않는다.

📁 `app/api/qa/route.ts:6-15`

### 4.3 "모르면 모른다고 해라"로는 부족하다 — 문구를 글자 그대로 못박아라

프롬프트에 "모른다고 답해라"만 적었더니 AI가 **"모릅니다" 같은 제 나름의 문장**으로 답해, 화면에 나와야 할 규정 문구가 안 나왔습니다.
근거 문서의 `- Q: / A:` 형식을 흉내 내 답변 앞에 "A: "를 붙이는 일도 있었습니다.

**해결**:
- 정확한 문자열을 **템플릿 리터럴로 프롬프트에 주입** — 코드 상수와 프롬프트가 어긋날 수 없게.
- "금액·수치는 문서에 적힌 값을 그대로 옮긴다. **계산하거나 반올림하지 않는다.**"
- `temperature: 0.2`
- **코드 레벨 게이트**: 검색 결과가 0건이면 OpenAI를 아예 호출하지 않고 고정 문구를 즉시 반환. 응답이 비면 같은 상수로 대체.

**원칙** — **프롬프트는 부탁이고, 코드가 계약이다.** LLM에게 지시할 때는 의도가 아니라 **출력물**을 지정하라.
그리고 컨텍스트에 넣은 텍스트는 내용뿐 아니라 **형식까지** 모델에 영향을 준다는 점을 기억하라.

📁 `app/api/qa/route.ts:63-66,104-132`

### 4.4 가공된 데이터 대신 원본을 써라

수가 문서를 만들려고 누군가 '치과 항목만 남겨' 정리해 둔 엑셀을 입력으로 썼는데,
**치면열구전색술·치면세마 같은 아주 흔한 급여 항목이 통째로 빠져 있었습니다.**

> 더 나쁜 것은 이 누락을 **파일만 봐서는 알아낼 방법이 없다**는 점입니다. 있어야 할 줄이 빠진 것은 화면에 아무 흔적도 남기지 않습니다.

이 상태로 위키를 만들었다면 직원이 검색해 '없다'는 답을 받고 **'이건 급여가 아니구나'라고 정반대로 이해**했을 것입니다.

**원칙** — 파이프라인의 입력이 이미 누군가의 판단으로 걸러진 것이라면, **그 판단은 검증할 수 없는 채로 최종 결과까지 전파된다.**
가능한 한 원본을 받아 걸러내는 규칙을 **코드로 소유하라.** 그리고 필터를 만들었으면 통과한 것뿐 아니라 **걸러낸 것도 눈에 보이게 출력하라** — 통과 목록만 보면 오탐만 잡히고, 정작 치명적인 **미탐은 영원히 모른다.**

📁 `scripts/extract-dental-fees.mjs`

### 4.5 RAG 품질의 절반은 코드가 아니라 '문서 작성 규칙'에서 나온다

청킹 전략을 정했으면 **그 전략에 맞는 집필 가이드**를 함께 만들어야 합니다. 실제로 가이드가 청킹 구현보다 **먼저** 작성됐습니다.

핵심 규칙들:
- 문서 하나에 주제 하나 (`보철과.md` ✗ → `보철과-임플란트2차수술.md` ✓)
- 공식 용어와 평소 쓰는 말 병기 (`지대주(어버트먼트)`)
- **각 절이 혼자서도 이해되게** (`## 주의사항` ✗ → `## 임플란트 2차 수술 주의사항` ✓)
- 모르는 건 비워두기 — 비워두면 "위키에 등록된 정보가 없습니다"가 나와 **더 안전하다**

그리고 규칙을 선의에 맡기지 않고 **등록 스크립트의 검증 단계로 강제**했습니다.
전 파일을 먼저 검사해 하나라도 문제가 있으면 *"고친 뒤 다시 실행해주세요. 아무것도 등록하지 않았습니다."* 로 중단합니다(반쯤 반영되는 상태 방지).

📁 `data/templates/README.md` · `scripts/seed-documents.mjs:44-76`

---

## §5. 프론트엔드 — 컴파일러가 잡아주지 않는 것들

### 5.1 props를 state에 복사할 때는 `useEffect` 대신 `key`로 리마운트

`useState`의 인자는 **최초 마운트 때 한 번만** 쓰입니다. props가 바뀌어도 반영되지 않습니다.
`useEffect`로 동기화하면 렌더 → 이펙트 → setState → 재렌더로 한 번 더 그려지고, 무엇보다 **리셋할 state를 하나라도 빠뜨리면 화면이 어긋납니다**(이 컴포넌트는 state가 10개였습니다).

```tsx
<DocumentDetail key={document.version} document={document} />
<NoteEditor key={selected.id} ... />
```

**공부 포인트** — React의 재조정은 같은 위치·같은 타입을 "같은 컴포넌트"로 보고 state를 유지합니다. `key`는 이 판단을 뒤집는 장치로, **"상태를 여기서 끊는다"는 선언**입니다.

**원칙** — 리셋해야 할 state가 두 개 이상이면 `key` 기반 리마운트가 거의 항상 더 안전하다 — **빠뜨릴 state가 존재하지 않기 때문**이다.
대가: 하위 트리의 DOM·포커스·스크롤이 초기화되고, 재조회 비용이 생긴다.

📁 `app/documents/[id]/page.tsx:34-37` · `components/notes/NotesApp.tsx:161,171`

### 5.2 `react-hooks/set-state-in-effect` — 호출한 함수 내부까지 따라 들어간다

`loadTree()` 같은 async 함수를 만들어 useEffect와 이벤트 핸들러에서 같이 부르려 했는데 린트가 **error**로 막았습니다.
직접 `setState`를 부르는 것뿐 아니라 **setState를 품은 함수를 호출하는 것까지** 잡아냅니다.

**우회 방법 두 가지**
1. 마운트 시 1회 조회 → 이펙트 안에서 `fetch().then(...)` 체인으로 **인라인**. setState가 이펙트 본문이 아니라 **프라미스 콜백 안**에 있으면 규칙에 걸리지 않습니다.
2. props 변화에 따른 리셋 → 아예 이펙트를 쓰지 않고 `key` 리마운트로.

**공부 포인트** — 이펙트 본문에서 바로 `setState`를 하면 브라우저가 화면을 그리기도 전에 다시 렌더 사이클이 도는 **cascading render**가 발생합니다. 반면 fetch의 `.then` 콜백 안 setState는 "외부에서 새 정보가 도착했을 때"라 정상 용법입니다.

**원칙** — 린트 규칙이 막으면 **"어떻게 끌까"가 아니라 "이 규칙이 무엇을 경고하는가"** 를 먼저 읽어라. 그리고 우회로 생긴 코드 중복에는 **반드시 이유를 주석으로 남겨라** — 다음 사람이 "중복이니 합치자"고 되돌리면 lint에서 터진다.

📁 `components/notes/NotesApp.tsx:23-54`

### 5.3 플렉스 자식에 `h-full`을 주면 오히려 높이가 무너진다

"세로로 꽉 채우려면 height:100%를 주면 되지"라는 직관으로 두 칼럼에 `h-full`을 줬더니, **656px짜리 부모 안에서도 칼럼이 내용 높이만큼 쪼그라들었습니다.**

**정답은 아무 높이도 주지 않는 것**이었습니다.

**공부 포인트** — 플렉스 아이템은 **cross-size가 `auto`일 때만** 부모의 `align-items: stretch`가 적용됩니다.
높이를 명시하는 순간 stretch 대상에서 빠져 퍼센트 계산으로 넘어가는데, `height:100%`는 "부모의 **확정된** 높이의 100%"라서 계산 순서가 어긋나면 `auto`처럼 동작합니다.

두 번째 함정은 `min-height`입니다. 플렉스 아이템의 기본값은 `0`이 아니라 **`auto`** 라서 내용보다 작아지지 못합니다.
내부 스크롤을 만들려면 **스크롤 영역까지 이어지는 모든 조상 플렉스 아이템에 `min-h-0`** 을 줘야 합니다.

**원칙** — CSS 레이아웃 문제는 추측하지 말고 **브라우저에서 계산된 높이를 실제로 측정**하라(이 프로젝트는 656px를 실측해 주석에 남겼습니다).

📁 `components/notes/NotesApp.tsx:113-155`

### 5.4 반응형이 깨지는 곳은 "누가 줄어드느냐"를 안 정한 flex다

375px에서 헤더 검색창이 **34px까지 찌그러지고** 가로 스크롤이 생겨 검색이 불가능했습니다.
로고와 우측 정보에만 `shrink-0`이 걸려 있어 **가운데 검색창만 모든 축소를 떠안았기** 때문입니다.

- 줄어들어야 하는 요소 → `min-w-0` (flex 자식의 기본 `min-width:auto` 하한을 푼다)
- 줄어들면 안 되는 요소 → `shrink-0`
- flex 안의 `w-full`은 **넘침의 원인** — `flex-1`로
- 넘칠 수밖에 없는 것(탭·표)은 **페이지 전체가 아니라 그 요소만** 가로 스크롤되게 격리

**원칙** — 반응형은 눈대중이 아니라 **실제 폭을 측정해** 검증하라(375/768/1280px에서 34px→279px 복구를 확인).

📁 `components/AppHeader.tsx` · `components/HeaderSearch.tsx:25`

### 5.5 "이미 실행했는가"(boolean)로 막으면 두 번째 입력이 무시된다

홈에 머문 채 헤더 검색창으로 다시 질문하면 URL의 `q`만 바뀌고 **컴포넌트는 리마운트되지 않습니다.**
그런데 자동 실행을 `hasAutoRun: boolean`으로 한 번만 막고 있어서, **두 번째 질문부터 아무 반응이 없었습니다.**

```ts
// ✗ const hasAutoRun = useRef(false);
// ✓ const lastRunQuestion = useRef<string | null>(null);
if (q && q !== lastRunQuestion.current) { lastRunQuestion.current = q; runSearch(q); }
```

**원칙** — boolean 플래그는 "이 **컴포넌트 생애** 동안 한 번"을 뜻하지 "이 **입력**에 대해 한 번"을 뜻하지 않는다.
**무엇이 이 실행을 유일하게 만드는가**를 값으로 명시하라.

📁 `components/QaScreen.tsx:16-24,48-58`

### 5.6 충돌(409)을 보여주는 방식은 "잃을 초안이 있는가"로 갈린다

- **텍스트 입력 중인 화면**: `router.refresh()`를 자동으로 하면 방금까지 타이핑한 본문이 통째로 날아갑니다.
  → 충돌 상태만 세우고 **입력 내용은 손대지 않고**, "새로고침" 버튼을 **사용자가 직접 누르게** 합니다.
  화면 문구: *"지금 작성 중인 내용은 그대로 있습니다. 새로고침하면 최신 내용을 볼 수 있지만, 지금 쓰던 내용은 사라집니다."*
- **버튼 클릭 하나뿐인 동작(되돌리기)**: 잃을 초안이 없으므로 곧바로 새로고침.

**원칙** — 에러 처리는 상태 코드별로 통일하는 게 아니라 **"사용자가 잃을 것이 있는가"** 로 갈라라.
사용자가 만든 데이터는 시스템이 임의로 버리지 않고, 버리는 행위는 **반드시 명시적 클릭으로만.**
에러 메시지는 세 가지를 담아야 한다: 무엇이 잘못됐나 / 내 데이터는 지금 어떤 상태인가 / 무엇을 하면 되나.

📁 `components/DocumentDetail.tsx:77-82,130-168` · `components/DocumentLogSection.tsx:23-29`

### 5.7 서버/클라이언트 경계는 취향이 아니라 **능력의 경계**

- 서버 컴포넌트는 **쿠키를 쓸 수 없다**(읽기만) → 세션 연장을 페이지에 넣을 수 없었음.
- Next.js 미들웨어는 **Edge 런타임 전용**이고 `node:crypto`가 없다 → "모든 요청에서 세션 갱신"이라는 흔한 설계를 쓸 수 없었음.
- `useSearchParams()`는 **Suspense 경계**를 요구한다.

**인증은 두 지점에서 각각 검사합니다** — 화면 진입(서버 컴포넌트 `redirect`)과 데이터 접근(API `401`).
**화면만 막으면 API가 뚫리고, API만 막으면 빈 화면이 뜹니다.**

**원칙** — 런타임 제약을 만나면 억지로 이식하기 전에 **"이 로직이 정말 모든 요청에서 돌아야 하는가"** 를 다시 물어라.
"모든 요청에서 연장"을 "의미 있는 조작에서만 연장"으로 좁히니 **재작성 없이 목적 대부분을 달성**했다.

📁 `lib/with-session.ts:5-27` · `app/settings/page.tsx`

---

## §6. API 설계 — 계층마다 자기 언어로

### 6.1 인증은 래퍼 한 곳에 모아 "빠뜨릴 수 없게" 만들어라

`withSession(handler)` 고차 함수가 세 가지를 합니다: 세션 확인(401) → 핸들러 실행 → 응답에 새 쿠키를 심어 만료 연장.
API 라우트 22개 중 **19개**에 적용돼 있고, 예외 셋은 이유가 분명합니다(로그인·로그아웃은 인증 이전 단계, purge-trash는 Cron 전용 인증).

미묘하지만 중요한 판단 하나:
> "이 요청이 401/403/400 등으로 끝났더라도, **세션 자체는 유효했으니 연장한다.** (권한이 없어 거부된 것과 로그인이 안 된 것은 다르다)"

**원칙** — 횡단 관심사를 래퍼로 강제하는 핵심 가치는 코드 절약이 아니라 **"빠뜨릴 수 없게 만드는 것"** 이다. 라우트마다 손으로 확인하면 언젠가 한 곳이 빠지고, **그 한 곳은 겉보기에 멀쩡하게 동작한다.**

### 6.2 DB 예외를 HTTP 상태코드로 옮기는 규칙

| 코드 | 의미 | 예 |
|---|---|---|
| **404** | 대상이 지금 이 사람에게 존재하지 않는다 | `NODE_NOT_FOUND`, uuid 형식 아님 |
| **409** | 요청은 옳은데 **서버 상태와 충돌**한다 | `VERSION_CONFLICT`, 중복 등록(23505) |
| **422** | 문법은 맞는데 **규칙상 처리 불가** | 휴지통에 있는 노트 수정, 금지 확장자, 용량 초과 |
| **400** | 요청 자체가 잘못됐거나 의미가 없다 | 필수 필드 누락, `NO_CHANGES`, 이미 그 상태 |

**원칙** — DB 계층은 **도메인 언어**(`NODE_NOT_FOUND`)로 실패하고, API 계층이 그것을 **HTTP 언어**(404)로 번역하라.
상태코드는 "누구 잘못인지"가 아니라 **"클라이언트가 다음에 무엇을 해야 하는지"** 로 갈라라 — 404는 포기, 409는 새로고침 후 재시도, 422는 다른 대상 선택, 400은 입력 수정.
그리고 매핑되지 않은 예외는 반드시 **500으로 떨어뜨려 남겨라.** 조용히 200을 주는 것보다 낫다.

### 6.3 큰 파일은 서버를 통과시키지 마라 — 3단계 업로드

```
① POST /attachments/upload-url  → 서버가 로그인·확장자·용량·부모 노트를 검사하고 서명 URL 발급
② 브라우저 → Storage 직접 PUT   → Vercel 함수를 전혀 거치지 않음(크기 제한 없음)
③ POST /attachments             → 실물 존재를 확인한 뒤에야 DB 등록 + 로그 기록(한 트랜잭션)
```

서버는 **"허가증을 발급하는 문지기"와 "결과를 확정하는 등기소"** 역할만 합니다.

**원칙** — 이 구조는 필연적으로 **중간 상태**("허가는 받았지만 안 올린", "올렸지만 확정 안 한")를 만든다.
그러므로 확정 단계에서 클라이언트의 주장을 검증하고, **고아 오브젝트를 청소할 계획을 처음부터 세워라.**
(→ 이 프로젝트는 아직 청소 코드가 없습니다. §9 참고)

### 6.4 API는 화면의 최종 모양이 아니라 **재료**를 내려줘라

트리를 서버에서 조립하지 않고 **평면 배열**로 한 번에 내려준 뒤 클라이언트에서 조립합니다.

- 첨부파일 목록은 트리를 거치지 않고 **원본 배열을 바로 필터링**
- 이름 변경 같은 부분 갱신은 `map` 한 줄
- 이름 검색도 왕복 없이 클라이언트에서

**공부 포인트** — Redux가 권하는 **정규화(normalization)** 와 같은 발상입니다. 데이터를 평면으로 두고 관계는 id로만 표현한 뒤, 화면에 필요한 모양은 그때그때 파생시킵니다.

**원칙** — 다만 이 선택은 **"전체를 한 번에 내려도 괜찮은 규모"** 라는 전제 위에 있다. 설계 결정에는 항상 **그 결정이 성립하는 규모를 함께 적어두라.**

📁 `lib/notes/tree.ts:18-53` · `app/api/notes/tree/route.ts`

---

## §7. 운영·배포·테스트

### 7.1 도구가 막히면 우회하되, 그 도구가 관리하던 '상태'는 유지하라

사내망에서 `supabase db push`가 쓰는 **Postgres 직결 포트(5432/6543)가 막혀** 타임아웃됐습니다.
대시보드 SQL 편집기에 손으로 붙여넣으면 **어떤 마이그레이션이 적용됐는지 아무도 추적할 수 없고**, 나중에 포트가 열린 환경에서 `db push`를 돌리면 같은 마이그레이션이 두 번 적용됩니다.

**해결**: Management API(HTTPS 443)로 SQL을 보내는 스크립트를 만들되, **CLI가 쓰는 것과 같은 장부에 기록**했습니다:
```sql
insert into supabase_migrations.schema_migrations (version, name)
values ('...', '...') on conflict (version) do nothing
```

**원칙** — 우회는 하되 **상태(장부)는 같은 형식으로 유지하라.** 장부를 갱신하지 않는 우회는 지금은 편하지만 표준 도구로 돌아갈 길을 끊는다.
그리고 적용 도구에는 **"지금 무엇이 적용됐고 무엇이 밀려 있는가"를 보여주는 status 모드**를 반드시 넣어라.

📁 `scripts/apply-migration.mjs`

### 7.2 테스트를 빌드에 묶어 배포 게이트를 만들되, 일부러 깨뜨려 확인하라

Vercel 자동 배포가 켜지면서 '배포 전 손 검증'이라는 관문이 사라졌습니다.
> "빌드는 문법 오류만 잡는다. '되돌리기가 제목까지 복원하는지' 같은 동작은 확인해주지 않아, 지금까지 배포 전 손 검증으로 메우던 관문이 사라진 상태였다"

```json
"build": "npm test && next build"
```
`&&`는 앞이 성공해야 뒤를 실행하므로, 테스트가 깨지면 빌드가 종료 코드 1로 끝나고 **Vercel이 배포를 취소해 이전 버전이 그대로 서비스**됩니다.

**게이트는 일부러 깨뜨려 확인했습니다** — *"일부러 isUuid에 버그를 넣어 빌드가 종료 코드 1로 차단되는 것을 확인했다."*
그리고 나중에 실전에서도 증명됐습니다 — *"이 실패로 빌드 차단 장치가 실제로 동작하는 것이 확인됐다. 배포가 중단되어 이전 버전이 그대로 서비스됐고 사이트는 정상이었다."*

**원칙** — 빌드에 넣는 자동 검증은 **빠르고 외부 의존이 없는 것만.** 느리거나 불안정하면 결국 사람이 꺼버린다.

### 7.3 로컬과 CI의 런타임 버전은 다르다

`"test": "node --test tests/unit/"` — 로컬 Node 26은 이를 '탐색할 디렉터리'로 해석했지만, **Vercel의 Node 24.19는 '실행할 모듈'로 해석**해 `MODULE_NOT_FOUND`로 실패했습니다. 로컬에서는 재현되지 않았습니다.

**해결**: 셸 글롭으로 파일 목록을 명시적으로 넘기기 — `node --test tests/unit/*.test.mts`

**원칙** — 런타임의 **탐색 기능에 기대지 말고** 대상을 명시적으로 넘겨라.

### 7.4 배포 확인은 상태 코드가 아니라 '내 변화'로 하라

Vercel CLI/API가 **SAML SSO로 막혀** 공식 확인 수단이 없었습니다. 대안으로 만든 curl 확인이 거짓말을 했습니다 —
**실제 응답 본문은 404 페이지인데 `%{http_code}`는 200으로 보고**되어, 폴링 루프가 영원히 돌았습니다.

**해결 두 갈래**
1. 배포 여부는 **커밋 기준**으로: `gh api repos/<owner>/<repo>/commits/<sha>/status`
2. 화면 확인은 상태 코드가 아니라 **본문에 있어야 할 문자열**로: `curl -sS <url> | grep -q '이번에 추가한 문구'`

**원칙** — **200은 "어떤 서버가 어떤 응답을 줬다"는 사실만 말한다.** 캐시된 옛 버전일 수도, CDN 에러 페이지일 수도, 소프트 404일 수도 있다.
"성공했는지" 판정 신호는 **내가 만든 변화와 직접 연결된 것**이어야 한다.
그리고 **모든 대기 루프에는 반드시 상한을 둬라** — 종료 조건이 틀릴 가능성을 전제하지 않은 루프는 언젠가 반드시 매달린다.

### 7.5 Cron이 부르는 엔드포인트는 사람용 인증으로 못 지킨다

Cron 호출에는 **세션 쿠키가 없습니다.** `withSession`으로 감싸면 Cron이 401을 맞고, 안 감싸면 **인터넷에 열린 '누구나 호출 가능한 전체 삭제 버튼'** 이 됩니다.

**해결**: 공유 비밀값(`CRON_SECRET`)을 Authorization 헤더로 받아 검증. **secret이 비어 있어도 401** — 설정 누락이 곧 무방비가 되지 않게.

**부분 실패**는 '건너뛰고 다음 실행에 재시도'로 처리했습니다. 실물 삭제가 실패한 노드는 DB 행을 남겨 **내일 Cron이 다시 집게** 합니다.

**원칙** — 배치는 '한 번에 다 끝난다'고 가정하지 마라. **재실행 가능(idempotent)** 하게 만들고, 실패한 건은 다음 회차로 미뤄라.

### 7.6 `.gitignore`의 `.env*`는 견본 파일까지 삼킨다

`.env*`를 적으면 `.env.example`도 함께 무시됩니다. `git add`를 해도 **아무 일도 일어나지 않고 오류도 안 나서**, clone한 사람은 "어떤 환경변수가 필요한지 적힌 파일이 없는" 상태가 됩니다.

```gitignore
.env*
!.env.example    # 부정 패턴은 반드시 광범위한 무시 패턴 다음에
```
줄바꿈 문제(윈도우·맥 혼용)는 `.gitattributes`에 `* text=auto eol=lf`로 고정했습니다.

**원칙** — **비밀값은 커밋하지 않되, '무엇이 필요한지'는 반드시 커밋한다** — 값과 목록은 다른 것이다.
한 번 커밋된 비밀값은 `.gitignore`를 나중에 추가해도 **히스토리에 영원히 남으므로**, 유출된 것으로 간주하고 재발급해야 한다.

---

## §8. AI와 함께 개발하기

### 8.1 규칙은 '대화로 부탁'하지 말고 '저장소에 커밋'하라

AI와 여러 세션에 걸쳐 개발하면 **세션마다 맥락이 끊깁니다.** 왜 이렇게 짰는지가 대화창에만 남고 저장소에 남지 않으면, 다음 세션의 AI(그리고 몇 달 뒤의 사람)가 같은 결정을 다시 하거나 반대로 뒤집습니다.

`CLAUDE.md`에 넣어 가장 값어치가 컸던 것은 취향이 아니라 **되돌릴 수 없는 사고를 막는 규칙**이었습니다:
- 삭제 금지 (복구 불가) — *"파일을 지워야 할 때는 `trash-can` 폴더로 옮겨만 둔다"*
- 비밀값 취급 (유출 불가역) — *"토큰 값을 채팅에 출력하지 말고 `.env`에서 읽어 쓴다"*
- 프레임워크 고정 (대규모 재작업 방지)
- **"모든 주석은 한국어로, 무엇을 왜 바꿨는지 적는다"**

### 8.2 '왜' 주석이 코드베이스를 개발일지로 만든다

이 저장소의 주석은 문법 설명이 아니라 **결정 기록**입니다. 마이그레이션 18개 중 11개가 `-- 왜 필요한가 / 왜 바꾸는가 / 어떻게 바꾸는가` 머리말을 갖습니다.

실제 예:
- 측정치까지: *"유사도가 0.27에 그쳐 문턱값 0.5를 넘지 못했다"*
- 합의 사실까지: *"(2026-09-04 실측 400~900ms) … 트레이드오프를 사용자와 합의하고 적용했다"*
- 검증 사실까지: *"(직접 확인함)"*
- 코드가 문서를 역참조: `// Design §5.1`, `// PRD 5번①`

**공부 포인트** — **'what 주석'** 은 코드를 읽으면 알 수 있어 코드가 바뀌면 곧 거짓말이 됩니다.
**'why 주석'** 은 코드에 절대 나타나지 않는 정보 — 버린 대안, 외부 제약, 측정한 수치, 합의한 트레이드오프 — 를 담아 **코드를 아무리 잘 짜도 대체할 수 없습니다.**
이를 문서로 정리한 관행이 **ADR(Architecture Decision Record)** 입니다.

### 8.3 '절대 만들지 않을 것' 목록이 경계를 준다

> AI는 빈칸을 만나면 그럴듯한 것으로 채웁니다.

`PRD.md`의 **비범위 9개 항목**이 실제로 세 번 방어선 역할을 했습니다 — Prisma 도입 기각, 저장소 추상화 계층 기각, DB 테스트 미작성.

핵심은 **항목 나열이 아니라 '왜 안 하는지'와 '언제 다시 검토할지'를 같이 적는 것**입니다.
> "저장소 추상화는 '나중에 용량이 부담되면 이전'이라는 **아직 확정되지 않은 미래 요구**를 위해 지금부터 인터페이스 계층을 만드는 것 … 지금은 그 파일 자체가 이미 유일한 저장소 접점이라 이전 비용이 크지 않다."

**공부 포인트** — YAGNI의 실무 판단 기준은 **'미래에 바뀔 확률'이 아니라 '바뀔 때의 변경 비용'** 입니다.
이 프로젝트는 "저장소를 바꿀지 모른다"가 아니라 **"바꿀 때 고쳐야 할 파일이 몇 개인가"** 를 물었고, 답이 `lib/file-storage.ts` 하나여서 추상화하지 않기로 했습니다.

**그리고 비범위는 영원한 금지가 아닙니다** — 초기 비범위였던 트리형 노트는 요구가 확인된 뒤 정식 편입됐습니다. 비범위는 *"지금은 하지 않는다 + 언제 다시 볼지"* 의 계약입니다.

### 8.4 문서와 코드가 다를 때, 고쳐야 할 쪽은 그때그때 다르다

Gap 분석(설계 문서 대 구현 대조)을 돌려보니 성격이 다른 두 종류가 섞여 있었습니다:
- **(A) 구현이 설계에 못 미친 진짜 결함** → 코드를 고침 (세션 활동 기반 연장, OpenAI try/catch)
- **(B) 구현이 설계보다 나아서 일부러 벗어난 것** → **문서를 고침**

(B)를 기계적으로 "코드를 문서에 맞춰라"로 처리했다면 **이미 의도적으로 막아둔 보안 구멍이 되살아났을 것**입니다:
> "관리자 여부를 세션에 저장하는 것은 이 세션 초반에 **일부러 없앤 보안 구멍**이다. 되돌리면 계정 삭제·관리자 강등이 최대 8시간 지연 반영되는 문제가 재발한다."

**원칙** — 설계 문서는 계약이 아니라 **그 시점의 최선의 이해**다. 문서와 코드가 다르면 *"어느 쪽이 틀렸나"* 가 아니라 **"어느 쪽이 지금 더 옳은가"** 를 물어라.
구현하면서 알게 된 사실(런타임 제약, 보안 구멍, 실측 성능)로 문서를 갱신하는 것은 **설계를 어긴 게 아니라 설계를 진전시킨 것**이다.
다만 **반드시 문서를 실제로 고쳐라** — 안 고치면 다음 사람이 문서를 믿고 코드를 되돌려 같은 버그를 재생산한다.

### 8.5 lint·build 통과 ≠ 화면이 돈다

한 커밋에서만 **lint·build를 멀쩡히 통과한 채 화면이 깨진 사례가 세 건** 나왔습니다:
> "업로드 확정 응답 파싱 버그, 2단 레이아웃이 뷰포트 밖으로 새는 CSS 버그, **검색 UI 자체가 안 붙어있던 누락**을 모두 발견해 그 자리에서 수정"

셋 다 컴파일러가 절대 잡을 수 없는 종류입니다. 특히 **기능이 통째로 빠진 것은 타입 오류를 내지 않습니다.**

| 검증 도구 | 잡는 것 |
|---|---|
| 린트 | 스타일, 명백한 실수 |
| 타입 검사 | 형태 불일치 |
| 유닛 테스트 | 계산 로직 |
| API 테스트 | 계약(상태 코드·인증) |
| **실제로 눌러보기** | **통합·레이아웃·누락된 기능** |

**원칙** — 초록불 몇 개를 봤다고 아래층 검증을 건너뛰지 마라.
그리고 '만들어야 할 화면을 아예 안 만든 누락'은 **어떤 자동 테스트로도 검출되지 않고**, 설계 문서의 체크리스트와 실제 화면을 사람이 대조해야만 발견된다.

### 8.6 회고 기록은 '사건 1개 = 교훈 1개'로 묶어라

이 문서를 만드는 과정에서 발견한 것이기도 합니다. 초안에서는 `window.prompt` 사건 하나가 **다섯 항목으로 흩어져** 있었습니다.

같은 사건을 제목만 바꿔 반복하면 세 가지 손해가 납니다:
1. 목록이 실제보다 풍부해 보여 **빠진 영역이 가려진다**
2. 읽는 사람이 **어느 항목이 정본인지 몰라 신뢰를 잃는다**
3. 내용을 고칠 때 **다섯 군데를 함께 고쳐야 한다**

**쪼갤 정당한 기준은 '독자가 다르거나, 재발 방지 조치가 다를 때'** 입니다.
prompt 사건에서 프론트엔드 개발자가 배울 것(네이티브 다이얼로그를 쓰지 마라)과 검증 담당자가 배울 것(스텁으로 대체한 것은 실물로 다시 확인하라)은 조치가 다르므로 나눌 만합니다.

---

## §9. 아직 남아 있는 숙제

아래는 이 정리 과정에서 발견한, **지금도 저장소에 남아 있는 문제들**입니다. 전부 직접 확인했습니다.

| # | 문제 | 위치 | 심각도 | 상태 |
|---|---|---|---|---|
| 1 | **죽은 분기 → 고아 파일** | `app/api/notes/purge-trash/route.ts` | 높음 | ✅ 2026-09-05 수정 |
| 2 | 확장자가 **차단 목록** 방식이라 `.html`·`.svg` 통과 | `lib/file-rules.ts` | 높음 | ✅ 2026-09-05 수정 |
| 3 | 확정 등록이 **클라이언트가 보낸 값**을 그대로 신뢰 | `app/api/notes/attachments/route.ts:15-17` | 중간 | ✅ 2026-09-06 수정 |
| 4 | Storage 버킷이 **코드 어디에도 없음** | 마이그레이션 부재 | 중간 | ✅ 2026-09-05 수정 |
| 5 | `window.confirm` 잔재 | `components/SettingsWhitelist.tsx` | 중간 | ✅ 2026-09-07 수정 |
| 6 | 안 쓰는 **문서 전체 임베딩**을 계속 생성 | `lib/document-write.ts:13` | 낮음 | ✅ 2026-09-06 해소 |
| 7 | 하이브리드 전환으로 **HNSW 인덱스 무용지물** | `20260904000701_hybrid_search.sql` | 낮음(미래) | ✅ 2026-09-07 해소 |
| 8 | Q&A 에 **요금 방어가 없다** | `app/api/qa/route.ts` | 중간 | ⏸ 2026-09-07 하지 않기로 함 |
| 9 | 첨부 미리보기가 **교차 출처 iframe** 인데 주석은 same-origin 이라고 적혀 있다 | `components/notes/MarkdownView.tsx` | 낮음 | ✅ 2026-09-07 수정 |
| 10 | 목록 조회에 **상한이 없다** | `notes/tree`·`notes/trash`·`documents/[id]/logs` | 낮음(미래) | 열림 |

**1. 죽은 분기 → 고아 파일** — 마이그레이션이 타입 이름을 `'image'` → `'attachment'`로 바꿨는데, 크론 라우트에는 아직 `node.type === "image"` 비교가 남아 있습니다.
DB에 `'image'` 행이 존재할 수 없으므로 **이 조건은 영원히 거짓**이고, Storage 실물 삭제가 절대 실행되지 않습니다.
결과: 30일 지난 첨부는 **DB 행만 사라지고 실물은 영구히 남습니다.**
TypeScript도 못 잡았습니다 — Supabase 클라이언트를 `Database` 제네릭 없이 만들어 `node.type`이 `string`으로 느슨하게 추론되기 때문입니다.
> **배울 점**: 이름 변경 리팩터링에서 진짜 위험한 것은 **컴파일러가 검사하지 않는 경계**(문자열 리터럴, SQL 값, 크론처럼 평소 안 도는 경로)다.
> 도메인 값은 유니온 타입 한 곳에서 정의하고, 이름을 바꿨으면 **옛 이름으로 전체 grep**하는 것을 리팩터링의 마지막 단계로 고정하라.
>
> **✅ 수정됨(2026-09-05)** — 조회 결과를 `NodeType`으로 좁혀 받도록 바꿔, 이제 `"image"`로 되돌리면 **빌드가 깨진다**(`TS2367: types 'NodeType' and '"image"' have no overlap`).
> 고치는 과정에서 **두 번째 버그가 드러났다**: 이 분기가 죽어 있던 덕분에 `continue`(재시도용 건너뛰기)가 한 번도 실행되지 않았는데, 분기를 살리자 비로소 도달 가능해졌다.
> `purge_nodes`는 "넘겨받은 목록 안에서 자식이 없는 것"만 리프로 보므로 **자식(첨부)만 빠지고 부모(노트)가 남으면 외래키 위반으로 그 회차 전체가 롤백**된다.
> 그래서 건너뛴 첨부의 **조상도 함께 다음 회차로 미루도록** 했다.
> — *교훈: 죽은 코드를 되살릴 때는 "이 코드가 실제로 돌기 시작하면 무엇이 처음으로 실행되는가"를 함께 봐야 한다.*

**2. 차단 목록의 한계** — 금지 목록에 `html`, `htm`, `svg`가 없습니다. HTML·SVG는 `<script>`를 담을 수 있고, 첨부는 서명 URL로 열리므로 **Supabase 도메인에서 스크립트가 실행**됩니다.
> **배울 점**: '나쁜 것을 나열해 막기'는 나열을 빠뜨리는 순간 뚫린다. 공격자는 목록에 없는 **하나만** 찾으면 되고 방어자는 **모든** 위험 형식을 알아야 하므로 이길 수 없는 싸움이다.
> 반대로 **허용 목록**은 빠뜨렸을 때 결과가 '보안 구멍'이 아니라 '기능이 안 됨'이라 반드시 신고로 드러난다.
>
> **✅ 수정됨(2026-09-05)** — `ALLOWED_EXTENSIONS = [png, jpg, jpeg, gif, webp, pdf]` 허용 목록으로 뒤집고, 확정 등록 단계에서 **mimeType도 같은 허용 목록**으로 함께 검사한다.
> 화면의 `accept` 속성도 같은 상수에서 생성해, **파일 선택창 필터와 서버 규칙이 어긋날 수 없게** 했다.

**3. 클라이언트 값 신뢰** — `sizeBytes`, `storagePath`, `mimeType`을 body에서 받아 그대로 검사·저장합니다.
콘솔에서 `sizeBytes: 1`을 보내면 50MB 제한을 그냥 통과합니다. **'두 번 검사'했지만 두 번 다 같은 거짓말을 검사한 셈**입니다.
> **배울 점**: 서버가 이미 알고 있거나 다시 계산할 수 있는 값은 **절대 요청 본문으로 받지 마라.** 제한은 물리적으로 강제할 수 있는 **가장 낮은 계층**(스토리지·DB 제약)에 두어야 진짜 제한이 된다.
>
> **✅ 수정됨(2026-09-06)** — 확정 등록이 본문에서 받는 값을 `noteId`·`attachmentId`·`name` 셋으로 줄이고, 나머지는 서버가 직접 구한다.
> `storagePath`는 발급 때와 **같은 규칙(`noteId/attachmentId`)으로 다시 만들고**, `sizeBytes`·`mimeType`은 **스토리지에 실제로 올라간 오브젝트에서 읽는다**(`getStorageObjectInfo`).
> 크기를 읽지 못하면 0으로 넘어가지 않고 **거부한다**(fail-closed) — 모르는 값을 기본값으로 메우면 제한이 다시 사라지기 때문이다.
> 용량은 버킷의 `file_size_limit`이 이미 물리적으로 막고 있다(`20260905120000`). 형식까지 버킷의 `allowed_mime_types`로 내리는 것은 **아직 하지 않았다** — 확정 등록을 거치지 않고 서명 URL로만 올리는 경로가 남아 있다는 뜻이다(§9-3 후속).
>
> **덤으로 드러난 것**: 경로를 서버가 만들게 하자 **노트 간 첨부 이식도 함께 막혔다.**
> 예전엔 `noteId`는 B로, `storagePath`는 A의 것으로 보내면 A에 올린 파일이 B의 첨부로 등록됐다. 지금은 경로가 `B/attachmentId`로 계산돼 오브젝트를 못 찾고 422가 난다.
> — *교훈: "받지 말고 계산하라"는 위조를 막는 데 그치지 않는다. 서로 맞물려야 하는 값들을 **하나의 출처에서 파생**시키면, 둘을 어긋나게 조합하는 공격 자체가 표현 불가능해진다.*

**5. `window.confirm` 잔재** — 화이트리스트 삭제 확인만 네이티브 다이얼로그를 쓰고 있었습니다. 노트 쪽은 같은 이유로 이미 화면 안 2단계 버튼으로 고쳐놓고, 설정 쪽만 남아 있었습니다.
> **배울 점**: 같은 원인으로 두 곳이 아픈데 한 곳만 고치면, 고친 쪽의 교훈이 저장소에 남아 있어도 안 고친 쪽은 계속 아프다. 원인을 고쳤으면 **그 원인을 쓰는 곳을 전부 grep** 해서 함께 정리하라.
>
> **✅ 수정됨(2026-09-07)** — 삭제 버튼을 누르면 "정말 지울까요? [삭제] [취소]" 로 바뀌는 2단계 방식으로 교체했다. `window.confirm` 호출은 저장소에서 0건이 됐다.

**6·7 은 지도 작업으로 저절로 풀렸습니다.**
> **6** — "아무도 안 읽는다"던 `documents.embedding` 을 `document_graph` 가 지도의 근거로 쓴다. 걷어낼 대상이 아니라 쓰임이 생긴 값이 됐다.
> **7** — `document_graph` 를 lateral 조인으로 바꾸면서 `documents_embedding_idx`(HNSW)를 실제로 탄다. `EXPLAIN` 으로 `Index Scan using documents_embedding_idx` 를 확인했다.
> — *교훈: "지금 안 쓰니 지우자"고 적어둔 것이 반년도 아니고 이틀 만에 쓰임이 생겼다. 지우기 전에 **왜 만들었는지**를 한 번 더 보는 편이 낫다.*

**8. Q&A 요금 방어 없음** — 로그인만 하면 `/api/qa` 를 무제한으로 부를 수 있고, 호출 한 번마다 OpenAI 임베딩 1회 + 답변 생성 1회가 나갑니다. 로그인 API 에는 시도 횟수 제한(사번 5회·IP 20회/10분)이 있는데 여기에는 없습니다. 세션 하나만 있으면 요금이 무제한으로 늘어납니다.
> **배울 점**: rate limit 을 "인증을 뚫는 문"에만 걸고 **"돈이 나가는 문"** 에는 안 거는 실수가 흔하다. 비용이 요청 수에 비례하는 경로는 인증 뒤에 있어도 한도가 필요하다.
>
> **⏸ 하지 않기로 함(2026-09-07)** — 한 번 만들었다가 되돌렸다. 사번당 10회/분·200회/일 한도를 DB 함수로 붙이고 동작까지 확인했지만, **"실제로 이만큼 질문할 사람이 없다"** 는 판단으로 걷어냈다. 적용했던 테이블·함수·마이그레이션 기록도 운영 DB 에서 모두 지웠다(쌓인 기록 0건).
>
> 되살릴 때 참고할 것 — 만들면서 정리했던 설계 판단들:
> - 로그인 가드는 **실패만** 기록하지만 Q&A 는 **성공해도 돈이 나가므로 모든 호출**을 세야 한다
> - 확인과 기록은 **한 함수 안에서** 해야 한다. 따로 두면 그 사이에 끼어들어 한도를 넘긴다
> - 한도는 IP 가 아니라 **사번 단위**여야 한다. 병원 내 같은 회선을 쓰는 동료들이 서로 때문에 막힌다
> - OpenAI **호출 전에** 소비해야 한다. 실패해도 돈은 나갔을 수 있고, 실패는 재시도를 부른다
>
> **다시 필요해지는 조건**: 스탭 수가 늘거나, OpenAI 요금이 예상 밖으로 오르거나, 세션이 유출된 정황이 있을 때. 그때는 위 판단들을 다시 쓰면 된다.

**9. 교차 출처 iframe** — 첨부 PDF 미리보기가 `<iframe src="/api/notes/{id}/content">` 인데, 그 라우트는 Supabase 서명 URL로 **307 리다이렉트**합니다. 즉 iframe 안은 `*.supabase.co` 입니다. 코드 주석에는 "same-origin 인증 라우트라 안전하다"고 적혀 있는데 사실과 다릅니다.
> 지금은 허용 목록(§9-2)이 html·svg 를 막고 있어 실제 위험은 없습니다. 다만 나중에 누군가 허용 형식을 늘릴 때 **이 주석을 근거로 안전하다고 판단하면** 그 순간 뚫립니다.
> **배울 점**: 주석에 적은 안전 근거가 틀리면, 코드가 멀쩡해도 **다음 사람이 잘못된 전제로 결정한다.** 근거는 코드만큼 정확해야 한다.
>
> **✅ 수정됨(2026-09-07)** — 그 라우트가 리다이렉트 대신 **바이트를 직접 흘려보내도록** 바꿔 진짜 same-origin 이 됐다. 서명 URL 도 브라우저에 노출되지 않는다. 응답에 `script-src 'none'` 을 걸어, 허용 목록이 뚫려 html·svg 가 들어와도 스크립트가 실행되지 않는다. 틀렸던 주석도 바로잡았다.
> 대신 파일 바이트가 우리 함수를 한 번 거치므로 대역폭과 실행 시간을 쓴다 — 리다이렉트 방식은 그 비용이 0이었다. 받아들인 교환이다.
> — *처음엔 `sandbox` 를 걸었다가 **브라우저 내장 PDF 뷰어까지 막혀** 미리보기가 빈 화면이 됐다. 위협 모델(업로드 파일 안의 스크립트)보다 넓게 막으면 막을 필요 없는 것까지 꺼진다. 그 실패는 오류도 경고도 없이 조용해서, 헤드리스 검사는 전부 초록불이었는데 사람 눈에는 빈 화면이었다.*

**10. 목록 조회 상한 없음** — `notes/tree`·`notes/trash` 는 조건에 맞는 행을 전부 내려주고, `documents/[id]/logs` 는 모든 수정 이력의 **본문 전문**을 함께 내려줍니다. 지금 규모(노트 11건, 로그 14건)에서는 문제가 없지만 상한이 어디에도 없습니다.

---

## §10. 다음 프로젝트 체크리스트

**시작할 때**
- [ ] 요구를 **명사가 아니라 상황**으로 물었는가? 벤치마크 대상을 물어봤는가?
- [ ] **비범위 목록**을 '이유 + 재검토 조건'과 함께 적었는가?
- [ ] AI 협업 규칙(삭제 금지·비밀값 취급·주석 언어)을 **저장소에 커밋**했는가?

**DB**
- [ ] 여러 테이블을 바꾸는 작업이 **한 트랜잭션** 안에 있는가?
- [ ] 감사 로그의 '행위자'가 **외래키가 아니라 스냅샷**인가?
- [ ] 동시 편집 가능한 자원에 **낙관적 잠금**이 있는가?
- [ ] 함수 시그니처를 바꿀 때 **옛 시그니처를 DROP**했는가?
- [ ] 제약을 좁히며 데이터도 바꾼다면 **DROP → UPDATE → ADD** 순서인가?

**보안**
- [ ] RLS **와** GRANT/REVOKE를 **둘 다** 걸었는가? DEFAULT PRIVILEGES까지?
- [ ] 아이디가 공개값인가? 그렇다면 rate limit이 있는가?
- [ ] rate limit 카운터가 **공유 저장소**에 있는가? 검사 실패 시 **fail-closed**인가?
- [ ] 토큰에 "즉시 반영돼야 하는 값"을 넣지 않았는가?
- [ ] 클라이언트 검증과 별개로 **서버 검증**이 있는가? 허용 목록 방식인가?
- [ ] 각 환경에 **그 환경이 실제로 쓰는 값만** 줬는가?

**RAG**
- [ ] 답이 틀렸을 때 **프롬프트가 아니라 검색 점수**를 먼저 쟀는가?
- [ ] 조각에 **상위 문맥(제목)** 을 붙였는가?
- [ ] 문턱값을 **실제 질문 세트로 측정**했는가? 산식을 바꾼 뒤 **다시** 쟀는가?
- [ ] 입력 데이터가 **원본**인가, 누군가 걸러낸 것인가?

**검증·배포**
- [ ] 스텁으로 대체한 것이 **검증하려던 위험 자체**는 아닌가?
- [ ] 빌드 게이트를 **일부러 깨뜨려** 확인했는가?
- [ ] 배포 확인이 **상태 코드가 아니라 내 변화**를 보고 있는가?
- [ ] 대기 루프에 **상한**이 있는가?
- [ ] 이름을 바꿨다면 **옛 이름으로 전체 grep**했는가?

---

## §11. 용어 사전

| 용어 | 뜻 | 이 프로젝트에서 |
|---|---|---|
| **트랜잭션** | 여러 변경을 '전부 되거나 전부 안 되거나'로 묶는 것 | PL/pgSQL 함수 호출 하나 = 트랜잭션 하나 (§2.1) |
| **낙관적 잠금** | 미리 잠그지 않고 저장 시점에만 충돌 확인 | `version` + `expectedVersion` → 409 (§2.3) |
| **RLS** | 행 수준 보안. "어떤 행이 보이나" | GRANT/REVOKE와 **다른 층** (§3.1) |
| **타이밍 공격** | 계산 시간 차이로 비밀을 알아내는 기법 | `timingSafeEqual` (§3.3) |
| **사용자 열거** | 응답 차이로 계정 존재 여부를 알아내는 것 | 실패 메시지 통일 (§3.3) |
| **fail-closed** | 검사기가 죽으면 '차단'이 기본값 | rate limit 확인 실패 → 503 (§3.4) |
| **서명 URL** | "N초 동안 이 파일 열람 허용"에 서명한 URL | 60초, 307 리다이렉트로 전달 (§3.8) |
| **저장형 XSS** | DB에 남은 악성 스크립트가 모든 사용자에게 실행 | react-markdown이 구조적으로 차단 (§3.7) |
| **임베딩** | 문장을 의미가 가까울수록 가까운 벡터로 | 1536차원, `<=>`가 코사인 거리 (§4) |
| **청킹** | 문서를 의미 단위로 쪼개 조각마다 임베딩 | `## ` 절 기준, 1200자 초과 시 문단 재분할 (사건 4) |
| **하이브리드 검색** | 벡터 + 키워드 점수를 합치는 것 | 0.6 : 0.4 가중합 (사건 4) |
| **재현율/정밀도** | 놓치지 않음 / 쓸데없는 걸 안 가져옴 | RAG는 재현율 쪽으로 기울인다 (§4.2) |
| **프롬프트 인젝션** | 입력에 지시문을 심어 규칙을 무력화 | 직원이 쓴 위키 본문이 프롬프트에 들어감 |
| **인접 리스트** | `parent_id`로 부모를 가리키는 트리 모델 | `nodes` 테이블, 재귀 CTE 필요 (§2.6) |
| **파생 데이터** | 원본에서 다시 만들 수 있는 데이터 | 조각·임베딩, `on delete cascade` (§2.7) |
| **재조정(React)** | 같은 위치·타입이면 같은 컴포넌트로 보고 state 유지 | `key`로 그 판단을 뒤집는다 (§5.1) |
| **멱등성** | 몇 번 실행해도 결과가 같은 성질 | 마이그레이션 장부, Cron 재시도 (§7.1, §7.5) |
| **IaC** | 인프라를 클릭이 아니라 코드로 선언 | Storage 버킷이 빠져 있음 (§9-4) |
| **ADR** | 결정의 맥락·선택·결과를 남기는 기록 | 마이그레이션 머리말 주석이 그 역할 (§8.2) |
| **YAGNI** | 지금 필요 없는 건 만들지 않는다 | 판단 기준은 '바뀔 확률'이 아니라 '변경 비용' (§8.3) |

---

*이 문서는 저장소의 커밋 24개·마이그레이션 18개·소스 코드 주석을 근거로 작성했습니다. 각 항목의 `📁` 경로를 직접 열어 확인해 보세요.*
