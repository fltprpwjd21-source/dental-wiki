# NEXT — 다음에 할 일 구상

2026-09-08 작성. PLAN.md가 "이번 사이클에 만들 것"이라면, 이 문서는 **그다음에 손댈 후보**를 
우선순위와 근거만 적어 둔 곳이다. 착수가 정해지면 PLAN.md나 docs/01-plan 으로 옮긴다.

> ⚠️ 지금 계정 2개로 세션을 동시에 굴리고 있다. 이 문서를 고칠 때는 상대 세션이 
> 같은 파일을 만지고 있지 않은지 먼저 확인한다.

---

## 0. 다른 작업보다 먼저 정리해야 하는 것

### 0-1. 마이그레이션 13개가 원격에 안 올라가 있다

```
로컬 33개 / 원격 19개 → 20260906120000 부터 미적용
```

지식 지도, 공지, 회의록 카테고리, 파일 제한 100MB 가 전부 로컬에만 있다. 
두 세션이 각자 마이그레이션을 만들다 보니 벌어진 일로 보인다. `supabase db push` 는 
밀린 것을 **전부 순서대로** 적용하므로, 하나만 골라 올릴 수 없다.

**하기 전에**: 상대 세션이 작업 중인 마이그레이션이 섞여 있는지 확인한다. 
작업 중인 것을 같이 밀면 반쯤 만든 스키마가 운영 DB에 올라간다.

**파일명 충돌 주의**: 새 마이그레이션을 만들 때 이미 같은 타임스탬프가 있는지 본다. 
실제로 `20260908120000` 이 양쪽에서 겹칠 뻔했다(`_notices` vs `_storage_bucket_limit`).

### 0-2. `acked_at` 을 지우는 마이그레이션과 코드가 어긋나 있다

`20260908150001_drop_notice_ack.sql` 이 `notice_reads.acked_at` 을 지우는데, 
`app/api/notices/[id]/read/route.ts:25` 는 아직 그 컬럼에 값을 쓴다. 
**이 마이그레이션을 적용하는 순간 「읽음 확인」 버튼이 500 으로 죽는다.**

지금은 미적용이라 멀쩡히 도는 상태다. 관련해서 같이 붕 떠 있는 것들:

- `components/notices/NoticeAck.tsx` — 아직 `app/notices/[id]/page.tsx:162` 에서 쓰인다
- `notices.needs_ack` 컬럼은 안 지워졌다 (지운 것은 `acked_at` 뿐)
- `app/api/notices/[id]/read/route.ts` 자체

상대 세션이 「읽음 확인」을 걷어내는 중이라면 그쪽에서 마무리될 일이다. 
**중간에 끼어들어 고치지 말고 먼저 확인할 것.** 아무도 안 하고 있으면 
라우트·컴포넌트·`needs_ack` 를 함께 정리한다.

### 0-3. Supabase 요금제 확인 (10초)

대시보드 → Organization → Billing.

무료 플랜은 **파일 하나당 50MB 가 플랫폼 상한**이라, 방금 올린 100MB 설정이 
실제로는 50MB 에서 계속 막힌다. 저장공간도 총 1GB 다. 
Pro 면 그대로 동작한다(파일당 50GB, 저장공간 100GB).

---

## 1. PPT 업로드 허용 — 준비 끝, 30분

보관함에 PPT 를 올리고 싶다는 요구에서 나왔다. **브라우저는 pptx 를 못 연다** 
(PDF 는 내장 뷰어가 있지만 pptx 는 없다). 자동 변환은 Vercel 에 LibreOffice 를 
깔 수 없어 불가능하고, MS·구글 온라인 뷰어는 파일을 인터넷에 공개해야 해서 탈락했다.

**결론: PDF 로 내보내 올리고, 원본 pptx 는 다운로드용으로 함께 둔다.**

| 파일 | 쓰임 |
|---|---|
| `발표자료.pdf` | 지금처럼 미리보기 + 새 창 (읽는 용도) |
| `발표자료.pptx` | 다운로드만 (고치는 용도) |

손댈 곳 세 군데:

1. `lib/file-rules.ts` — 허용 목록에 `pptx` 추가
   - 구형 `.ppt` 와 매크로 포함 `.pptm` 은 **넣지 않는다**. 허용 목록을 쓰는 이유가 
     "빠뜨렸을 때 보안 구멍이 아니라 안 올라감으로 드러난다" 인데 굳이 넓힐 이유가 없다
   - MIME: `application/vnd.openxmlformats-officedocument.presentationml.presentation`
2. `app/api/notes/[id]/content/route.ts` — 미리보기가 안 되는 형식은 
   `Content-Disposition` 을 `inline` 대신 `attachment` 로. 
   지금대로 두면 새 창에 빈 화면이 뜬다
3. `components/notes/NoteEditor.tsx` — 첨부 목록 아이콘을 형식별로 나눈다. 
   지금은 이미지가 아니면 전부 `📄` 라, 눌러보기 전에는 미리보기가 되는지 알 수 없다

**용량**: 사진 많은 PPT 는 100MB 도 넘길 수 있다. 넘으면 제한을 더 올리기보다 
원본을 구글 드라이브에 두고 링크만 적는 쪽이 맞다 (아래 "원본은 구글에" 원칙).

---

## 2. 문서와 구현이 벌어진 곳 메우기

기능은 만들어졌는데 기획·설계 문서가 못 따라온 자리들이다. 
Q&A 가 이 문서들을 근거로 답하는 서비스라 특히 방치하면 안 된다.

- **공지 기능이 PRD 에 아예 없다.** 카드뉴스·캘린더·읽은 사람 집계까지 만들어져 
  도는데 PRD 5번에도 6번 범위에도 한 줄이 없다. 가장 큰 구멍
- **`DESIGN.md` 의 "카테고리별 탐색 화면" 행이 낡았다.** 아직 
  "진료과별 인수인계 / 수가 / 내규 3개 탭" 으로 적혀 있는데, 실제로는 인수인계를 
  빼고 회의록을 넣었다(같은 문서의 "탭" 행에는 반영돼 있다). PRD 45·67행은 이미 갱신됨
- **`docs/01-plan`·`docs/02-design` 에 `file-server`·`notes` 것만 있다.** 
  공지·지식 지도·기공물은 plan/design 문서가 없다

---

## 3. 기공물장부 리뉴얼 — 방향부터 정할 것

`app/lab/page.tsx` 는 28줄짜리 자리만 잡아둔 상태다(PRD ⑥ 설명 그대로).

**주의: PRD ⑥ 의 명세("구글 시트를 원본으로 두고 읽어서 D-day 로 보여준다")가 
더 이상 최신 의도가 아니다.** 2026-09-08 에 "기공물장부 자체를 리뉴얼할 필요가 있다"고 
정해졌고 방향은 미정이다. **그대로 구현하지 말고 무엇을 바꾸고 싶은지부터 확인한다.**

---

## 4. 보류 중 — 재개할 때 읽을 것

### 4-1. 구글 캘린더 연동 (PRD ⑦, 개발 단위 6차 23~27)

PRD 갱신까지 끝내고 **구글 콘솔 세팅 직전에 멈췄다.** 재개하면 23번부터다.

정해진 것:
- **서비스 계정 + 캘린더 공유** 방식. 직원에게 구글 로그인을 요구하지 않는다
- 다루는 것은 직원 근무·시프트·휴가·당직뿐. 환자 예약은 넣지 않는다
- 원본은 구글 캘린더에만 둔다. 우리 DB 에는 "어느 캘린더를 볼지" 설정만 저장한다
- 조회는 전 스탭, 등록·수정·삭제는 관리자

PRD 에 없는, 재개할 때 필요한 사실:
- **캘린더는 이미 운영 중이다.** 구글 계정 하나 아래 **사람 이름별로** 나뉘어 있고 
  필요한 사람 것만 켜서 보는 방식으로 쓰고 있다. 이 구조를 갈아엎지 말고 
  휴가·교육회의 같은 종류별 캘린더만 옆에 추가하기로 합의했다
- **그 계정이 Workspace 인지 일반 지메일인지 아직 모른다.** 이게 난이도를 가른다 — 
  Workspace 면 도메인 전체 위임으로 공유 작업이 0회, 일반 지메일이면 캘린더마다 
  하나씩 서비스 계정에 공유해야 한다(사람 수만큼 반복)
- **권한을 캘린더마다 다르게 준다.** 앱이 쓸 캘린더(휴가·교육회의)만 "변경 권한", 
  사람별 근무 캘린더는 "일정 세부정보 보기" 만. 앱 버그로 남의 근무가 지워지는 걸 막는다
- **공유 전에 사람별 캘린더에 환자 이름이 섞여 있는지 확인한다.** 있으면 그 캘린더는 제외

**화면 설계가 처음 구상과 달라져야 한다.** 별도 `/schedule` 화면을 새로 만들려 했는데, 
앱에 **이미 캘린더 UI 가 있다**(공지 날짜용). `DESIGN.md` 의 "캘린더 표시색(예약)" 항목이 
이미 이 상황을 내다보고 **"근무표를 얹을 때는 주황을 쓰지 말고 파랑 계열로"** 라고 
못 박아 뒀다. 새 화면이 아니라 **기존 캘린더에 층으로 얹는 쪽**이 이 프로젝트의 설계 의도다.

### 4-2. 모바일 알림 (PRD ⑧, 개발 단위 7차 28~33)

표준 웹 푸시(VAPID)로만. 파이어베이스·알림톡·SMS 는 비범위.

- 정기 알림(그날 휴가·당직자)은 **캘린더 연동이 먼저 있어야 보낼 내용이 생긴다**
- 중요 공지 즉시 알림은 **공지 기능이 이미 있어서 붙일 자리가 마련돼 있다.** 
  다만 "중요 공지" 표시가 필요한데, `notices.needs_ack` 를 재활용할지 새로 넣을지는 
  0-2 의 정리 결과를 보고 정한다
- **아이폰은 홈 화면에 추가(설치)해야만 알림이 온다**(iOS 16.4+). 사파리 탭으로 열어두면 
  안 온다. 안내 UI 가 반드시 필요하다
- 발송 트리거는 이미 있는 Vercel Cron + `CRON_SECRET` 패턴을 그대로 쓴다

### 4-3. 자연어로 일정 등록 — 보류 유지

"다음주 수요일부터 휴가" 같은 문장을 해석해 캘린더에 넣는 기능. 
제미나이 무료 API 로 가능하고 PRD 비범위에 보류 사유와 함께 적어 뒀다. 
**캘린더 연동이 자리를 잡은 뒤 다시 판단한다.** 
할 경우 LLM 이 캘린더를 직접 건드리게 하지 말고, 구조화된 결과를 뱉게 한 뒤 
**확인 카드를 거쳐** 쓴다 — 날짜 오해석이 곧 근무표 사고이기 때문이다.

---

## 5. 정식 출시 전 (PLAN.md 에 이미 있음)

테스트 데이터·로그 정리. 로그 테이블에는 수정·삭제를 막는 트리거가 걸려 있어 
비울 때만 소유자 권한으로 껐다가 같은 트랜잭션에서 되돌린다. 자세한 내용은 PLAN.md 참고.

---

## 굳어진 원칙 — 새 기능을 붙일 때 어긋나지 않게

- **원본은 바깥에, 앱은 보여주기만.** 기공물은 구글 시트, 근무 일정은 구글 캘린더, 
  큰 PPT 원본은 구글 드라이브. 두 곳에 같은 것을 두면 어느 쪽이 맞는지 알 수 없어진다
- **환자 식별 정보는 우리 DB 에 넣지 않는다.** 화면에 스쳐 보여주고 끝낸다
- **전송량이 저장공간보다 먼저 바닥난다.** 첨부는 열 때마다 Supabase·Vercel 양쪽에서 
  파일 크기만큼 전송량이 발생한다(캐시 60초). 용량 제한을 올리는 것은 근본 해법이 아니다
- **주황은 「공지에서 지정한 날짜」 전용.** 캘린더에 다른 것을 얹을 때 재사용하지 않는다
- **허용 목록은 넓히지 않는다.** 빠뜨렸을 때 보안 구멍이 아니라 "안 올라감" 으로 드러나는 
  쪽이 안전하다
