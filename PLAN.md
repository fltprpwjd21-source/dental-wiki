# PLAN

## 이번 사이클 목표
PRD.md에 정의된 치과위키 기능 전체(로그인, Q&A 검색, 문서 수정·로그·되돌리기, 카테고리별 탐색, 관리자 설정)를 순서대로 구현해 MVP를 완성한다.

## 성공 기준
- Must 기능(질문-답변 검색, 문서 수정·로그·되돌리기) 두 가지가 실제로 동작한다
- Nice 기능(카테고리별 탐색, 설정 탭 관리자 옵션)까지 포함해 PRD 5번 기능 전체가 구현된다
- 등록되지 않은 사원번호로는 로그인이 되지 않고, 모든 문서 수정이 로그로 남는다 (PRD 4·7번 요건)
- `npm run lint`, `npm run build`가 오류 없이 통과한다

## 작업 목록 (순서대로 — 먼저 되면 나머지가 쉬워지는 순으로 정렬)
1. Supabase(Postgres)에 문서·카테고리·수정로그·사원번호 화이트리스트(관리자 여부 포함) 데이터 구조 설계, 문서에는 pgvector 기반 의미 검색용 임베딩 컬럼 포함
2. 사원번호 화이트리스트 시딩 및 로그인 API 구현 (로그인 성공 시 세션 발급 포함)
3. 로그인 화면 UI 구현 (로그아웃 버튼 포함)
4. 기존 인수인계·수가·내규 문서를 마크다운으로 정리해 시드 스크립트로 초기 입력 (입력 시 임베딩도 함께 생성, 자동 변환 도구는 만들지 않음)
5. 질문 → 임베딩 기반 의미 검색(벡터 유사도)으로 위키 문서를 찾는 RAG API 구현
6. 질문-답변 화면 UI 구현 (답변 + 근거 문서 표시)
7. 문서 수정·등록 API 구현
8. 등록·수정 시 자동 로그 기록 기능 구현 (최초 등록도 하나의 로그로 남김)
9. 새 문서 등록·수정 화면 UI 구현
10. "수정 로그 보기" 펼치기 UI 구현
11. 이전 버전으로 되돌리기 기능 구현
12. 문서에 카테고리 필드 부여 및 분류
13. 카테고리별 탐색 화면 UI 구현
14. 설정 탭 UI 구현
15. 설정 탭 내 관리자용 사원번호 등록·삭제 기능 구현 (세션의 관리자 여부를 확인해 접근 제한)
