# PLAN

## 이번 사이클 목표
PRD.md에 정의된 치과위키 기능 전체(로그인, Q&A 검색, 문서 수정·로그·되돌리기, 카테고리별 탐색, 관리자 설정)를 순서대로 구현해 MVP를 완성한다.

## 성공 기준
- Must 기능(질문-답변 검색, 문서 수정·로그·되돌리기) 두 가지가 실제로 동작한다
- Nice 기능(카테고리별 탐색, 설정 탭 관리자 옵션)까지 포함해 PRD 5번 기능 전체가 구현된다
- 등록되지 않은 사원번호로는 로그인이 되지 않고, 모든 문서 수정이 로그로 남는다 (PRD 4·7번 요건)
- `npm run lint`, `npm run build`가 오류 없이 통과한다

## 작업 목록 (순서대로 — 먼저 되면 나머지가 쉬워지는 순으로 정렬)
1. Supabase(Postgres)에 문서·카테고리·수정로그·사원번호 화이트리스트(관리자 여부 포함) 데이터 구조 설계, 문서에는 pgvector 기반 의미 검색용 임베딩 컬럼 포함
2. 사원번호 화이트리스트 시딩 및 로그인 API 구현 (로그인 성공 시 세션 발급 포함)
3. 로그인 화면 UI 구현 (로그아웃 버튼 포함)
4. 기존 인수인계·수가·내규 문서를 마크다운으로 정리해 시드 스크립트로 초기 입력 (입력 시 임베딩도 함께 생성, 자동 변환 도구는 만들지 않음)
5. 질문 → 임베딩 기반 의미 검색(벡터 유사도)으로 위키 문서를 찾는 RAG API 구현
6. 질문-답변 화면 UI 구현 (답변 + 근거 문서 표시)
7. 문서 수정·등록 API 구현
8. 등록·수정 시 자동 로그 기록 기능 구현 (최초 등록도 하나의 로그로 남김)
9. 새 문서 등록·수정 화면 UI 구현
10. "수정 로그 보기" 펼치기 UI 구현
11. 이전 버전으로 되돌리기 기능 구현
12. 문서에 카테고리 필드 부여 및 분류
13. 카테고리별 탐색 화면 UI 구현
14. 설정 탭 UI 구현
15. 설정 탭 내 관리자용 사원번호 등록·삭제 기능 구현 (세션의 관리자 여부를 확인해 접근 제한)

## 정식 출시 전 정리 (2026-09-08 결정)
지금은 화면과 기능을 만드는 중이라 테스트로 넣은 데이터와 그때 쌓인 로그가 섞여 있다.
정식 출시 시점에 한 번 비우고 시작한다.

- `documents`·`nodes`·`notices` 의 테스트 데이터 정리
- `document_logs`·`node_logs`·`file_logs` 를 비운다
  - 두 로그 테이블에는 수정·삭제를 막는 트리거가 걸려 있다(PRD 5번②).
    비울 때만 DB 소유자 권한으로 트리거를 껐다가 같은 트랜잭션에서 되돌린다.
    앱에서 로그를 지우는 길은 만들지 않는다.
- `notice_reads` 도 함께 비운다 (읽은 사람 집계가 테스트 기록부터 시작하지 않게)

---

# PLAN — 업무지시 (8차, 2026-09-09 결정)

## 목표
지시자 1명이 담당자 여러 명에게 업무를 내리고, 담당자 전원이 하나의 업무지시를 공유하며
진행 기록과 첨부파일을 함께 쌓고, 지시자가 「완료 확인」으로 종결하는 탭을 만든다.
(PRD ⑨ 참고)

## 성공 기준
- 담당자 여러 명을 지정한 업무지시 하나를, 담당자 전원이 같은 화면에서 보고 같은 진행 기록·첨부파일을 쌓는다
- 「확인」 체크는 담당자 개인별로 남고, 지시자 화면에 「확인 2/3」처럼 보인다
- 담당자가 「완료 보고」를 올리면 지시자 화면 맨 위로 올라가고, 지시자가 「완료 확인」을 눌러야 완료 목록으로 내려간다
- 지시자·담당자가 아닌 사람은 그 업무지시를 열 수 없다 (API 에서 403)
- `npm run lint`, `npm run build`가 오류 없이 통과한다

## 설계 결정 (2026-09-09 사용자 확정)
1. **담당자는 여러 명.** 사람별 사본이 아니라 지시 하나를 공유한다. 따라서 진행률·진행 기록·첨부파일은
   업무지시당 하나이고, 개인별로 갈리는 것은 「확인」 체크뿐이다.
2. **지시는 로그인한 누구나** 낼 수 있다 (관리자 전용 아님). 공지를 누구나 올릴 수 있게 한 것과 같다.
3. **첨부 형식은 현행 유지** (png·jpg·gif·webp·pdf·pptx). `lib/file-rules.ts` 는 2026-09-05 에
   허용 목록 방식으로 뒤집은 파일이라 손대지 않는다. 엑셀·한글 요구가 실제로 나오면 그때 다시 판단한다.
4. **완료는 2단계** (완료 보고 → 완료 확인). PRD 비범위의 "승인/결재 절차"에 해당하지 않는다 —
   문서 내용을 승인받는 절차가 아니라 지시자가 자기 업무를 종결하는 것이다.
5. **`task_seen` 테이블은 만들지 않는다.** 공지에서 쓰던 "안 본 항목" 추적이 필요 없다.
   "완료 확인을 아직 안 눌렀다"가 곧 "최상단에 있어야 한다"이므로 상태값 하나로 충분하다.
6. **반려는 사유 입력을 강제한다.** 사유 없이 되돌리면 담당자는 무엇을 고쳐야 할지 알 수 없다.
   반려하면 (가) 사유가 진행 기록으로 남고 (나) 완료 보고를 올린 담당자에게 알림이 가고
   (다) 진행률이 0으로 초기화되고 (라) 상태가 `in_progress` 로 돌아가 다시 완료 보고를 할 수 있다.
   초기화되는 것은 `tasks.progress` 현재값뿐이고, 그동안의 진행률은 `task_updates.progress` 에
   그대로 남는다 — 장기 업무에서 몇 달치 진행이 사라진 것처럼 보이면 안 되기 때문이다.
7. **진행 기록은 본문만 고칠 수 있고 지울 수는 없다.** 문서 로그처럼 통째로 잠그지
   않는 이유는 진행 기록이 기계가 남긴 로그가 아니라 사람이 쓴 글이기 때문이다 —
   오타 하나로 영원히 남는 건 과하다. 반대로 「완료 보고」·「반려」는 상태를 바꾼
   사건이고 "누가 언제 완료했다고 했는지"의 근거라 아예 못 고치게 막는다.
   작성자·시각·진행률도 본문과 함께 잠근다. 고치면 `edited_at` 이 남는다.
   유일한 삭제 경로는 지시 자체를 완전 삭제할 때 함께 정리되는 것뿐이다.
8. **알림은 앱 안의 표시로만 한다.** 웹 푸시(PRD ⑧)는 아직 미구현이다. 지금은 반려 배지를
   완료 보고자에게 띄우고, 웹 푸시가 붙을 때 발송 대상 하나를 추가하면 된다.
9. **열람은 지시자·담당자로 제한한다.** 앱의 다른 기능(전 스탭 공유)과 다른 유일한 지점이다.
   업무지시에는 질책성 내용이 들어갈 수 있고, 나중에 여는 것은 쉬워도 이미 본 것을 되돌릴 수는 없다.

## 상태 전이

    assigned ──진행 기록 등록──▶ in_progress ──완료 보고──▶ submitted ──완료 확인──▶ done
                     ▲                                          │
                     └── 반려 (사유 필수 · 진행률 0 · 보고자 알림) ──┘

- `assigned` 아직 진행 기록이 하나도 없음
- `in_progress` 진행 기록이 하나 이상 올라옴
- `submitted` 담당자가 완료 보고함 — 지시자 화면 최상단
- `done` 지시자가 완료 확인함 — 완료 목록으로 내려감
- 「확인」 체크는 상태값이 아니라 `task_assignees.acked_at` 에만 남는다 (담당자마다 다르므로)
- 완료 보고는 담당자 누구나 할 수 있다. 누가 보고했는지 카드에 표시한다.
- 진행률은 `is_longterm = true` 인 지시에만 표시한다.
- 반려는 지시자만 할 수 있고 사유가 필수다. 반려 시 `tasks.progress = 0`, `status = in_progress`,
  `submitted_at = null` 로 되돌리고 `kind='reject'` 진행 기록을 남긴다.
  같은 지시를 몇 번이든 다시 완료 보고할 수 있다 (횟수 제한을 두지 않는다).

## 데이터 구조

    create type task_status as enum ('assigned','in_progress','submitted','done');

    tasks
      id, title, body,
      assigner_id / assigner_name        -- 스냅샷 (퇴사해도 누가 시켰는지 남는다)
      status, is_longterm, progress      -- progress 는 최신 진행 기록의 캐시값. 반려 시 0 으로 되돌린다
      submitted_by                       -- 완료 보고를 올린 담당자. 반려 알림이 갈 대상이다
      due_on, submitted_at, completed_at,
      created_at, updated_at, deleted_at -- 휴지통 7일 (TRASH_RETENTION_DAYS 재사용)

    task_assignees   (task_id, employee_id) pk
      employee_name  -- 스냅샷
      acked_at            -- 「확인」 체크. 담당자별로 갈린다
      rejection_seen_at   -- 반려 배지를 언제 봤는지. 최신 reject 기록보다 이르면 배지를 띄운다

    task_updates     -- 담당자 전원과 지시자가 함께 쌓는 진행 기록
      id, task_id, author_id, author_name, body,
      progress,      -- 이 기록 시점의 진행률 (안 바꿨으면 null)
      kind,          -- 'note' | 'submit' | 'reject' — 반려 사유도 기록으로 남긴다
      created_at

    task_attachments
      id, task_id, update_id(null 이면 지시문 첨부),
      name, size_bytes, mime_type, storage_path, uploaded_by, created_at

- 전부 RLS 켜고 `anon, authenticated` 에서 revoke — 서버(서비스 롤)로만 접근한다 (공지와 동일)
- 스토리지 경로는 `{taskId}/{attachmentId}` — `file-server` 버킷과 `lib/file-storage.ts` 를 그대로 쓴다
- `task_assignees (employee_id)` 인덱스 — "내가 해야 할 업무" 조회용

## 화면 `/tasks`
- 상단 「내가 내린 업무」 — `assigner_id = 나`. 상태 뱃지, 확인 n/m, 진행률 바, D-day
- 하단 「내가 해야 할 업무」 — `task_assignees.employee_id = 나`
- 정렬: ① `submitted`(완료 확인 대기) → ② 마감 임박 → ③ 최근 갱신순
- `done` 은 접어둔 「완료된 업무」 섹션으로 분리
- 반려된 지시는 담당자 화면에 사유 배너를 띄운다. 배지는 완료 보고를 올린 담당자에게만 붙고,
  그 사람이 지시를 열면(`rejection_seen_at` 갱신) 사라진다. 사유 자체는 진행 기록이므로 담당자 전원이 본다
- 지시 작성창에 환자 개인정보 금지 안내를 상시 표시

## 작업 목록 (PRD 개발 단위 8차 34~40번과 같음)
34. ✅ 마이그레이션 — 테이블 4개 + enum 2개 + RLS revoke + 진행 기록 보호 트리거
    (`20260910150000_tasks.sql`, `20260910160000_task_updates_purgeable.sql`, 2026-09-10 적용)
35. ✅ `lib/tasks.ts` — 상태 전이 규칙과 권한 판정 + 단위 테스트 32건 (2026-09-10)
36. ✅ 목록·등록 API + 확인용 화면 (2026-09-10)
    - `GET/POST /api/tasks`, `lib/tasks-server.ts`, `/tasks` 화면, 탭 추가
    - 담당자 선택은 이름 검색으로 거른다. 직원 목록은 화면이 서버에서 직접 읽는다
      (설정 화면이 화이트리스트를 읽는 방식과 같다 — 별도 API 를 두지 않았다)
    - 39·40번의 목록·정렬·완료 접기까지 여기서 함께 만들었다. 남은 것은 카드 안의
      조작 버튼(확인·진행 기록·완료 보고·완료 확인·반려)이며 37번에서 붙인다
37. ✅ 확인 체크 / 진행 기록 / 완료 보고 / 완료 확인 / 반려 + 대화창 UI (2026-09-10)
    - `POST /api/tasks/[id]/ack` · `/updates`(기록·완료 보고) · `/decision`(완료 확인·반려)
    - `components/tasks/TaskThread.tsx` — 시간순 말풍선, 상태 변화는 가운데 시스템 줄
    - 진행률 슬라이더 20% 단위 6칸(장기 업무만), 글 없이 진행률만 올려도 기록으로 남는다
    - 초안은 브라우저(localStorage)에 자동 임시저장 — 올린 기록은 지울 수 없으므로
      올리기 전 초안까지 서버에 둘 이유가 없다. 대신 다른 기기에서는 이어 쓸 수 없다
    - 탭 배지 = 내가 지금 눌러야 할 것(미확인 받은 업무 + 완료 확인 대기 + 안 본 반려).
      `status='submitted'` 자체가 "확인·반려 전"이라 알림용 표를 따로 만들지 않았다
38. 첨부파일 API — 보관함의 2단계 서명 URL 방식 복제
39. 화면 UI — 내가 내린 업무 + 내가 해야 할 업무
40. 정렬·완료 목록 분리, `AppTabs.tsx` 에 탭 추가
