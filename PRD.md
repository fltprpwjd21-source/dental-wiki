# 치과위키 서비스 기획서 (PRD)

- 작성자: DS
- 작성일: 2026-08-28

## 1. 추진 배경 (문제 정의)

치과위생사, 기공사, 방사선사는 진료과별로 계속 바뀌는 인수인계 정보, 건강보험 수가와 비보험 항목, 병원 내규·운영회칙 등을 업무 중 수시로 확인해야 한다. 그러나 이런 정보가 문서·구두 전달 등으로 흩어져 있고 지속적으로 갱신되다 보니, 필요한 순간에 최신 정보를 빠르게 찾기 어렵고, 신규 인력 온보딩이나 근무자 간 인수인계 과정에서 정보가 누락되거나 잘못 전달되는 경우가 발생한다.

## 2. 현행 방식과 한계

현재는 진료과별 인수인계 사항을 파워포인트 문서로 작성해 전달하고 있고, 보험 수가·비보험 항목은 엑셀 파일이나 인쇄물로 별도 관리하며, 병원 내규·운영회칙은 종이 문서나 개별 공유 파일 형태로 흩어져 있다. 이 때문에 최신 버전이 무엇인지 헷갈리고, 필요한 정보를 찾으려면 여러 사람에게 물어보거나 여러 파일을 뒤져야 하며, 담당자가 자리를 비우거나 퇴사하면 정보가 유실되기도 한다.

## 3. 목표와 기대효과 (성공 기준)

- 정보 검색 소요 시간: 원하는 인수인계·수가·내규 정보를 찾는 데 걸리는 시간을 평균 5분 이내로 단축
- 재처방률 감소: 보험 적용 여부(급여/비급여)를 잘못 판단해 재처방하는 비율을 월 30% 이상 감소 (앱 내 자동 집계는 하지 않으며, 병원에서 별도로 수기 집계해 도입 전후를 비교)
- 정보 최신성: 각 진료과 인수인계 문서가 갱신 후 1영업일 이내에 위키에 반영되는 비율 90% 이상

## 4. 사용자와 이용 흐름

**사용자**: 치과위생사, 기공사, 방사선사

**이용 흐름**
1. 사용자가 궁금한 내용을 자연어로 질문 입력 (예: "임플란트 2차 수술 비급여 항목 뭐있어?")
2. → AI가 위키에 등록된 인수인계·수가·내규 문서에서 관련 내용을 검색
3. → 가장 최신 내용을 기본으로 보여주며, 근거가 된 문서를 함께 표시
4. → 답변이 틀렸거나 갱신이 필요하면, 사용자가 별도 승인 없이 나무위키처럼 바로 문서를 수정/등록
5. → 모든 수정은 "누가, 언제, 무엇을 고쳤는지" 로그로 자동 기록
6. → 문서 화면에서는 최신 버전을 기본으로 보여주되, "수정 로그 보기"를 펼치면 이전 수정 이력을 확인 가능

## 5. 주요 기능 (must / nice로 구분)

**Must**

① 질문-답변 검색 (RAG 기반 Q&A)
- AI가 지킬 규칙: 위키에 등록된 문서 내용에서만 답변을 생성한다. 위키에 근거가 없는 내용은 추측하지 않고 "위키에 등록된 정보가 없습니다"라고 답한다. 답변에는 반드시 근거 문서명(출처)을 함께 표시한다.

② 문서 수정·등록 + 자동 로그 기록 + 되돌리기
- AI가 지킬 규칙: 사용자가 문서를 수정하면 승인 절차 없이 즉시 반영한다. 모든 수정 건에 대해 수정자, 수정 시각, 변경 전/후 내용을 로그로 자동 기록하며, 기록된 로그는 수정·삭제할 수 없다. 사용자는 로그에 남은 이전 버전을 선택해 현재 버전으로 되돌릴 수 있으며, 되돌리기 역시 하나의 수정 이력으로 자동 기록된다. 단, 문서를 완전히 삭제하는 기능은 제공하지 않는다.

**Nice (꼭 필요하지만 must 2개 범위 밖)**

③ 카테고리별 탐색
- AI가 지킬 규칙: 모든 문서는 반드시 "회의록 / 보험·비보험 수가 / 내규·운영회칙" 3가지 카테고리 중 하나로 분류되어야 하며, 사용자가 질문 없이도 카테고리를 선택해 해당 문서 목록을 바로 탐색할 수 있어야 한다.

④ 설정 탭 내 관리자 옵션 (사원번호 화이트리스트 관리)
- AI가 지킬 규칙: 사원번호 화이트리스트에는 각 사원번호마다 "관리자 여부"를 함께 저장해두고, 로그인한 사원번호가 관리자로 표시된 경우에만 설정 탭이 보이고 접근할 수 있다. 일반 스탭 계정에는 설정 탭 자체가 보이지 않는다. 관리자는 전용 관리 계정 하나(`00001`)로 고정하고, 화면에서는 관리자 권한을 새로 부여할 수 없다 — 그 외 계정은 전부 동일한 일반 권한이다. 목록에는 "관리자 여부" 대신 **이름**을 표시해, 사원번호만 보고는 누구인지 알 수 없던 문제를 없앤다. (2026-09-07 변경) 이름은 **필수**다 — 등록 화면·API·DB 제약 세 곳이 함께 강제하며, 빈 이름이나 공백뿐인 이름으로는 계정을 만들 수 없다. (2026-09-10 변경)

⑤ 트리형 노트(메모장)
- 위키 Q&A로 답변하기엔 맞지 않는 자료 — 원본 그대로 보관하고 싶은 설명 글과 사진이 섞인 자료(예: 진료과별 세부 인수인계 자료) — 를 다루기 위한 별도 기능이다. 옵시디언(Obsidian)의 "폴더 트리 = 노트 모음" 방식을 벤치마킹했다.
- AI가 지킬 규칙: 상위/하위 폴더를 자유롭게 만들 수 있는 트리 구조를 제공하고, 폴더 안에 마크다운 노트를 만들어 화면에서 직접 쓰고 고칠 수 있게 한다. 노트에 사진을 올리면 업로드 즉시 화면에 보여야 한다. 화면은 왼쪽에 폴더·노트 트리, 오른쪽에 선택한 노트의 보기/편집 화면을 두는 2단 구성으로 만든다. 노트 저장에도 문서와 동일하게 동시 편집 충돌 감지(낙관적 잠금)를 적용하고, 삭제는 완전 삭제가 아니라 휴지통(30일 뒤 자동 완전삭제)으로 처리하며, 생성·수정·삭제·복구는 모두 수정·삭제할 수 없는 로그로 남긴다. 폴더·노트는 전 스탭이 함께 보고 편집하는 공유 공간이며(사원번호별로 나뉜 개인 노트가 아님), 폴더·노트 생성은 관리자로 제한하지 않고 로그인한 스탭 누구나 가능하다.

⑥ 기공물확인 (2026-09-08 추가, 미구현)
- AI가 지킬 규칙: 기공소와 주고받는 기공물 목록은 구글 시트에 그대로 두고, 앱은 읽어서 보여주기만 한다. 도착 예정일이 지난 것을 맨 위로 올리고 예정일을 D-day로 바꿔 보여준다. 기공물 시트에는 환자 이름·차트번호가 들어 있으므로 우리 DB에 저장하지 않는다 — 화면을 열 때 읽어 보여주고 끝낸다. 지금은 탭 자리와 색만 잡아둔 상태다.

⑦ 근무 일정 캘린더 — 구글 캘린더 연동 (2026-09-08 추가, 미구현)
- 다루는 것은 직원의 근무·시프트·휴가·반차·당직 일정뿐이다. 환자 예약 일정은 넣지 않는다 (비범위의 개인정보 원칙을 그대로 유지한다).
- AI가 지킬 규칙: 근무 일정의 원본은 구글 캘린더 하나로만 두고 우리 DB에 복제하지 않는다 — 두 곳이 어긋나면 어느 쪽이 맞는지 알 수 없게 되기 때문이다(기공물확인에서 구글 시트를 원본으로 둔 것과 같은 이유다). 앱은 화면을 열 때 구글 캘린더를 읽어 보여주고, 등록·수정도 구글 캘린더에 바로 쓴다. 연결은 구글 **서비스 계정**에 해당 캘린더를 "변경 및 공유 관리 권한"으로 공유하는 방식을 쓰며, 직원 개개인에게 구글 로그인을 요구하지 않는다 (앱 로그인은 사원번호 방식 그대로다). 조회는 로그인한 전 스탭이 가능하고, 등록·수정·삭제는 관리자만 가능하다. 모든 요청이 서비스 계정 명의로 나가 구글 캘린더에서는 작성자가 구분되지 않으므로, 일정마다 비공개 속성에 등록한 사원번호를 남겨 "누가 넣은 일정인지"를 보존한다.

⑧ 모바일 알림 (2026-09-08 추가, 미구현)
- AI가 지킬 규칙: 표준 웹 푸시(Web Push, VAPID)로만 구현하고, 별도 앱이나 파이어베이스 같은 외부 푸시 서비스를 쓰지 않는다. 알림은 두 종류다 — (가) 매일 정해진 시각에 그날 휴가·당직인 사람을 요약해 보내는 정기 알림, (나) "중요 공지"로 표시된 문서가 등록되면 즉시 보내는 알림. 정기 알림은 이미 쓰고 있는 Vercel Cron + `CRON_SECRET` 방식을 그대로 따른다. 알림 수신은 개인이 설정에서 켜고 끌 수 있으며 기본값은 꺼짐이다. 아이폰은 사파리로 접속한 상태에서는 알림이 오지 않고 홈 화면에 추가(설치)해야만 동작하므로(iOS 16.4 이상), 화면에 그 안내를 함께 제공한다.

⑨ 업무지시 (2026-09-09 추가, 미구현)
- 한 사람(지시자)이 여러 사람(담당자)에게 업무를 내리고, 담당자가 확인·진행·완료를 남기면 지시자가 종결하는 기능이다. 업무지시 하나를 담당자 전원이 **공유**한다 — 진행 기록도 첨부파일도 담당자 모두가 함께 보고 함께 올린다. 사람마다 쪼개진 사본이 아니다.
- AI가 지킬 규칙: 지시는 로그인한 스탭 누구나 낼 수 있다(관리자 전용이 아니다). 담당자는 여러 명 지정할 수 있고, 「확인」 체크만 담당자 개인별로 남긴다 — 누가 아직 안 봤는지 알아야 하기 때문이다. 진행률과 진행 기록·첨부파일은 업무지시 하나에 하나로 공유한다. 진행률은 **장기 업무로 표시한 지시에만** 나타난다 — 단기 업무에까지 퍼센트 입력을 요구하면 아무도 쓰지 않는다(공지의 「읽음 확인」 버튼을 2026-09-09 에 걷어낸 것과 같은 이유다). 완료는 두 단계다: 담당자가 「완료 보고」를 올리면 지시자 화면 맨 위로 올라가고, 지시자가 「완료 확인」을 눌러야 종결되어 완료 목록으로 내려간다. 지시자는 완료 보고를 반려할 수 있다 — 반려에는 사유 입력을 반드시 요구하고(사유 없는 반려는 받는 사람이 무엇을 고쳐야 할지 알 수 없다), 사유는 진행 기록으로 남으며, 완료 보고를 올린 담당자에게 알림이 간다. 반려하면 진행률은 0으로 초기화되고 상태는 진행 중으로 돌아가, 담당자가 다시 「완료 보고」를 올릴 수 있다. 초기화되는 것은 현재 진행률 값뿐이며 그동안의 진행률 이력은 진행 기록에 그대로 남는다. 알림은 웹 푸시(⑧)가 붙기 전까지 앱 안의 표시로만 처리한다. 열람은 그 지시의 지시자와 담당자에게만 허용한다(앱의 다른 기능과 달리 전 스탭 공유가 아니다). 첨부 가능한 형식은 보관함과 같은 목록(png·jpg·gif·webp·pdf·pptx)을 그대로 쓴다. 지시문에는 환자 이름·차트번호를 적지 않으며, 작성 화면에 그 안내를 상시 표시한다.

## 6. 범위와 비범위

**범위 (이번에 만들 것)**
- 회의록 / 보험·비보험 수가 / 내규·운영회칙 3개 카테고리의 문서 등록·조회
  - 「진료과별 인수인계」 카테고리는 2026-09-08 에 뺐다 — 설명 글과 사진이 섞인 인수인계 자료는 보관함(폴더 트리)에서 꺼내 쓰는 쪽이 맞았다
- 자연어 질문 → 위키 문서 검색 → 근거 문서와 함께 답변하는 Q&A
- 나무위키 스타일 직접 수정 기능 + 수정자·시각·변경 내용 자동 로그 기록 및 "로그 보기" 펼치기
- 이전 버전으로 되돌리기(롤백) 기능
- 카테고리별 탐색 UI
- "설정" 탭 내 관리자 옵션으로 사원번호 화이트리스트를 등록·삭제·이름 수정하는 기능 (화이트리스트에 이름 필드와 관리자 여부 필드 포함, 관리자 여부는 관리 계정 하나에만 고정)
- 기존 인수인계·수가·내규 문서를 마크다운으로 정리해 시드 스크립트로 직접 입력하는 초기 데이터 이관 (자동 변환 도구는 만들지 않음)
- 상위/하위 폴더로 정리되는 트리형 노트(메모장): 마크다운 직접 작성·편집, 사진 첨부 즉시 표시, 전 스탭 공유, 휴지통(30일) 방식 삭제
- 문서 간 의미 관계를 자동으로 계산해 보여주는 지식 지도: 메인 화면 배너에는 보기 전용으로, 돌려보기·문턱값 조절·고립 문서 점검은 별도 화면(`/map`)에서 (2026-09-06 추가)
  - 2026-09-07 변경: 지도 화면을 **관리자 전용에서 전 스탭 공개로** 바꿨다. 지도가 보여주는 것은 문서 제목·분류·연결 세기뿐이고 문서 자체는 원래 전 스탭이 읽고 고칠 수 있으므로(아래 비범위의 "역할별 권한 세분화 없음"), 권한을 열어도 새로 드러나는 정보가 없다. 오히려 "내가 찾는 내용이 어느 문서 근처에 있나"를 보는 쓸모가 전 스탭에게 있다.
  - 2026-09-07 변경: 지도 화면을 **3차원**으로 만들었다. 2차원에서는 점과 선이 겹쳐 읽기 어려웠고, 특히 이름표를 상시로 그려야 해서 글자끼리 부딪혔다. 3차원에서는 이름표를 선택했을 때만 띄운다. 메인 배너는 보기 전용이라 2차원 그대로 둔다(WebGL 을 전 스탭 메인 화면에 얹지 않기 위해서다).
- 구글 캘린더와 연동한 직원 근무·휴가·당직 일정의 조회와 등록·수정 (원본은 구글 캘린더에 두고 앱 DB에는 복제하지 않음) (2026-09-08 추가)
- 휴대폰으로 받는 웹 푸시 알림: 그날의 휴가·당직자 정기 알림, 중요 공지 즉시 알림 (2026-09-08 추가)
- 업무지시: 지시자 1명 → 담당자 여러 명, 담당자별 확인 체크와 담당자 전원이 공유하는 진행 기록·첨부파일, 장기 업무 진행률, 「완료 보고 → 완료 확인」 2단계 종결 (2026-09-09 추가)

**비범위 (절대 만들지 않을 것)**
- 환자 개인정보·진료기록 등 개인 식별 정보가 포함된 문서 저장·검색
- 문서 수정에 대한 승인/결재 절차 (업무지시의 「완료 확인」은 여기 해당하지 않는다 — 문서 내용을 승인받는 절차가 아니라, 지시자가 자기가 낸 업무를 종결 처리하는 것이다)
- 문서 완전 삭제 기능 (수정만 가능하며 이전 내용은 로그로 보존)
- 일반 문서 열람·수정에 대한 역할별 권한 세분화 (스탭 전원 동일 권한, 단 화이트리스트 관리자 화면과 업무지시 탭은 예외 — 업무지시는 그 지시의 지시자·담당자만 열람한다) (2026-09-09 업무지시 예외 추가)
- 재처방률 등 지표의 자동 집계·통계 대시보드 (수치는 앱 밖에서 수기로 확인)
- 모바일 전용 앱 개발 (반응형 웹으로만 대응)
- 여러 병원/지점을 지원하는 멀티테넌시 기능
- 노트 기능에서의 위키링크·백링크(사람이 `[[노트이름]]`을 직접 달아 잇는 방식 — 옵시디언의 대표 기능이지만 이번 범위에서 제외), 실시간 공동 편집, 옵시디언 앱과의 자동 동기화
  - 2026-09-06 정정: "그래프뷰"는 이 줄에서 뺐다. 사람이 링크를 다는 방식은 여전히 범위 밖이지만, **이미 검색에 쓰고 있는 문서 임베딩으로 관계를 자동 계산해 그리는 지식 지도는 범위에 넣기로 했다**(위 범위 항목 참고). 링크를 아무도 달지 않으면 그래프가 비는 문제를 피하면서 같은 효과를 얻는다.
- 노트 기능에서의 폴더·노트별 세분화된 접근 권한 (전 스탭 동일 권한)
- 환자 예약 일정의 조회·등록 (캘린더 연동은 직원 근무 일정에만 한정한다) (2026-09-08 추가)
- 자연어로 일정을 등록하는 기능 ("다음주 수요일부터 휴가야" 같은 문장을 자동 해석해 캘린더에 넣는 것) — 2026-09-08 논의했으나 이번 범위에서는 보류하고, 캘린더 연동이 자리를 잡은 뒤 다시 판단한다
- 카카오 알림톡·SMS 등 유료 발송 채널과 네이티브 앱 푸시 (웹 푸시로만 대응) (2026-09-08 추가)

## 7. 보안·개인정보 검토

- **공개등급**: 병원 내부 스탭(치과위생사, 기공사, 방사선사)만 접근 가능한 내부용 서비스로, 외부에는 공개하지 않는다.
- **로그인 방식**: 아이디는 개인 사원번호, 비밀번호는 전 직원 공통 비밀번호를 사용한다. 단, 사전에 등록된 사원번호 목록에 있는 경우에만 로그인이 허용되도록 하여, 공통 비밀번호를 알더라도 등록되지 않은 사원번호로는 외부인이 접속할 수 없게 한다. 로그인한 사원번호는 그대로 사용자 식별자로 쓰여 수정 로그에 "누가 고쳤는지" 정확히 남는다. 화이트리스트에는 이름과 관리자 여부도 함께 저장해, 목록에서 사원번호가 누구인지 알아볼 수 있게 하고 관리자로 표시된 사원번호만 설정 탭에 접근할 수 있게 한다. 관리자는 관리 계정 하나뿐이다.
- **사람 표시**: 화면에 사람이 나오는 곳(문서 수정 이력, 보관함 기록, 공지 작성자, 관리자 휴지통, 헤더, 업무지시의 지시자·담당자)은 **어디서나 실명으로 표시한다**. 사원번호는 로그인과 설정 탭의 화이트리스트 화면에서만 쓴다. 표시 규칙은 한 곳(`lib/employee-names.ts`)에 모으고, 지금 이름 → 기록에 남은 당시 이름 → 사원번호 순으로 떨어진다. 화이트리스트에서 이름을 고치면 옛 기록에도 새 이름이 반영되고, 퇴사해서 계정이 지워진 사람은 기록에 남은 당시 이름으로 계속 보인다. (2026-09-10 추가)
- **로그인 유지**: 로그인에 성공하면 서버가 세션(쿠키)을 발급해 로그인 상태를 유지한다. 로그아웃하거나 일정 시간 활동이 없으면 세션이 만료되어 다시 로그인해야 한다. 설정 탭 등 관리자 전용 화면은 매 요청마다 세션에 담긴 관리자 여부를 확인해 접근을 제한한다.
- **개인정보 여부**: 환자 개인정보·진료기록 등 개인을 식별할 수 있는 정보는 어떤 문서에도 포함하지 않는다. 등록 시 개인정보가 포함되지 않았는지 작성자가 확인한다.
- **API 키 관리**: LLM API 키 등 민감한 값은 `.env` 파일에만 저장하고 `.gitignore`에 등록해 GitHub에 올라가지 않도록 하며, 서버 코드에서만 사용하고 클라이언트(브라우저)에는 절대 노출하지 않는다.
- **구글 캘린더 연동 키 관리**: 구글 서비스 계정의 비공개 키는 `.env`에만 보관하고(줄바꿈이 깨지지 않도록 base64로 저장) 서버 코드에서만 사용한다. 서비스 계정에는 근무 일정 캘린더 하나만 공유하며, 그 외 구글 계정 데이터에는 접근 권한을 주지 않는다.
- **알림 구독 정보**: 웹 푸시 구독 정보는 개인의 기기를 가리키는 값이므로, 사원번호와 함께 저장하되 로그아웃하거나 화이트리스트에서 삭제된 사원번호의 구독 정보는 함께 지운다.

## 8. 기술 스택

- **Next.js**: 화면(프론트엔드)과 서버 쪽 코드(API)를 한 프로젝트에서 함께 처리한다.
- **고정 이유**: STEP 2에서 `my-app`에 이미 설치되어 있고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어서 가기 위함이다.
- **Google Calendar API**: 근무 일정의 저장소로 사용한다. 서비스 계정 JWT로 인증하며, 별도 SDK 없이 Node 내장 `crypto`와 `fetch`로 호출한다 (기존 OpenAI 호출과 같은 방식).
- **Web Push (VAPID)**: 휴대폰 알림 발송에 사용한다. 브라우저 표준 기능이라 외부 푸시 서비스와 별도로 계약할 필요가 없다.

## 개발 단위

**1차 — 로그인 + Q&A**
1. 문서/카테고리/로그/사원번호 데이터 구조(DB 스키마) 설계
2. 사원번호 화이트리스트 목록 등록 및 로그인 API 구현
3. 로그인 화면 UI 구현
4. 기존 인수인계·수가·내규 문서 초기 데이터 입력
5. 질문 → 위키 문서 검색(RAG) API 구현
6. 질문-답변 화면 UI 구현 (답변 + 근거 문서 표시)

**2차 — 수정·로그·되돌리기**
7. 문서 수정·등록 API 구현
8. 수정 시 자동 로그 기록 기능 구현
9. 문서 수정 화면 UI 구현
10. "수정 로그 보기" 펼치기 UI 구현
11. 이전 버전으로 되돌리기 기능 구현

**3차 — 카테고리별 탐색**
12. 문서에 카테고리 필드 부여 및 분류
13. 카테고리별 탐색 화면 UI 구현

**4차 — 설정 탭 관리자 옵션**
14. 설정 탭 UI 구현
15. 관리자용 사원번호 등록·삭제 기능 구현

**5차 — 트리형 노트(메모장)**
16. 폴더·노트·이미지 트리 데이터 구조(DB 스키마) 설계
17. 폴더·노트 생성, 트리 조회 API 구현
18. 노트 저장(동시 편집 충돌 감지 포함) API 구현
19. 이미지 업로드 및 노트 내 표시(비공개 저장소 + 인증 경유) API 구현
20. 휴지통 이동(하위 항목 포함)·복구·자동 완전삭제 API 구현
21. 2단 레이아웃(트리 + 보기/편집) 화면 UI 구현
22. 노트 이름 검색 UI 구현

**6차 — 근무 일정 캘린더**
23. 구글 클라우드 서비스 계정 발급 및 근무 일정 캘린더 공유 설정 (`.env`에 키·캘린더 ID 등록)
24. 서비스 계정 인증과 일정 조회/등록/수정/삭제를 담당하는 공통 모듈 구현
25. 기간별 일정 조회 API 구현
26. 일정 등록·수정·삭제 API 구현 (관리자 제한, 등록한 사원번호 기록 포함)
27. 근무 일정 화면 UI 구현 (주간 보기)

**7차 — 모바일 알림**
28. 웹 푸시 구독 정보 데이터 구조(DB 스키마) 설계 및 VAPID 키 발급
29. 서비스 워커 등록과 알림 권한 요청·구독 저장 구현 (설정에서 켜고 끄기 포함)
30. 알림 발송 공통 모듈 구현
31. 그날의 휴가·당직자 정기 알림 구현 (Vercel Cron)
32. 문서에 "중요 공지" 표시 추가 및 등록 시 즉시 알림 구현
33. 아이폰 홈 화면 추가 안내 UI 구현

**8차 — 업무지시**
34. 업무지시 데이터 구조(DB 스키마) 설계 — `tasks` / `task_assignees` / `task_updates` / `task_attachments` + 수정·삭제 불가 로그
35. 상태 전이 규칙 모듈(`lib/tasks.ts`) 구현 및 단위 테스트
36. 업무지시 목록·등록 API 구현 (담당자 다중 지정, 지시자·담당자 이름 스냅샷)
37. 확인 체크 / 진행 기록 등록 / 완료 보고 / 완료 확인·반려 API 구현
38. 업무지시 첨부파일 업로드·조회 API 구현 (보관함의 2단계 서명 URL 방식 재사용)
39. 업무지시 화면 UI 구현 — 상단 「내가 내린 업무」 데이터창, 하단 「내가 해야 할 업무」
40. 완료 보고 강조·완료 목록 분리 및 탭 추가
